<!DOCTYPE html>
<html lang="en" class="dark">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Shirtify - Premium Admin Dashboard</title>

    <!-- Performance optimizations -->
    <link rel="dns-prefetch" href="//cdn.tailwindcss.com">
    <link rel="dns-prefetch" href="//unpkg.com">
    <link rel="dns-prefetch" href="//fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>

    <!-- Modern fonts -->
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;800&display=swap"
        rel="stylesheet">

    <!-- Chart.js for data visualizations -->
    <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>

    <!-- Tailwind CSS -->
    <script src="https://cdn.tailwindcss.com"></script>

    <!-- Lucide icons -->
    <script src="https://unpkg.com/lucide@latest/dist/umd/lucide.js" defer></script>

    <!-- Modern CSS with clean design -->
    <style>
        /* Modern CSS Reset and Base Styles */
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        html {
            scroll-behavior: smooth;
        }

        body {
            font-family: 'Inter', sans-serif;
            -webkit-font-smoothing: antialiased;
            -moz-osx-font-smoothing: grayscale;
            background: #f8fafc;
            color: #1e293b;
            overflow-x: hidden;
            min-height: 100vh;
        }

        /* Card Styles */
        .glass-card {
            background: white;
            border: 1px solid #e2e8f0;
            border-radius: 8px;
            box-shadow: 0 1px 3px 0 rgba(0, 0, 0, 0.1);
            transition: all 0.2s ease;
        }

        .glass-card:hover {
            box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.1);
        }

        /* Form Elements */
        .modern-input {
            background: white;
            border: 1px solid #d1d5db;
            border-radius: 6px;
            padding: 8px 12px;
            color: #1e293b;
            transition: all 0.2s ease;
        }

        .modern-input:focus {
            border-color: #3b82f6;
            box-shadow: 0 0 0 3px rgba(59, 130, 246, 0.1);
            outline: none;
        }

        /* Stats Cards */
        .stat-card {
            background: white;
            border: 1px solid #e2e8f0;
            border-radius: 8px;
            padding: 20px;
            transition: all 0.2s ease;
        }

        .stat-card:hover {
            box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.1);
        }

        /* Sidebar */
        .sidebar {
            background: white;
            border-right: 1px solid #e2e8f0;
            transition: all 0.2s ease;
        }

        .sidebar-item {
            padding: 10px 14px;
            margin: 4px 8px;
            border-radius: 6px;
            transition: all 0.2s ease;
            cursor: pointer;
        }

        .sidebar-item:hover {
            background: #f1f5f9;
        }

        .sidebar-item.active {
            background: #eff6ff;
            color: #3b82f6;
            font-weight: 500;
        }

        /* Button Styles */
        .btn-primary {
            background: #3b82f6;
            color: white;
            transition: all 0.2s ease;
        }

        .btn-primary:hover {
            background: #2563eb;
        }

        .btn-primary:active {
            transform: scale(0.98);
        }

        /* Responsive Design */
        @media (max-width: 768px) {
            .sidebar {
                transform: translateX(-100%);
            }

            .sidebar.open {
                transform: translateX(0);
            }

            .main-content {
                margin-left: 0;
            }
        }

        /* Accessibility */
        @media (prefers-reduced-motion: reduce) {
            * {
                animation-duration: 0.01ms !important;
                animation-iteration-count: 1 !important;
                transition-duration: 0.01ms !important;
            }
        }

        /* Focus states for accessibility */
        .btn-primary:focus-visible,
        .modern-input:focus-visible,
        .sidebar-item:focus-visible {
            outline: 2px solid #3b82f6;
            outline-offset: 2px;
        }

        /* Custom scrollbar */
        ::-webkit-scrollbar {
            width: 8px;
        }

        ::-webkit-scrollbar-track {
            background: #f1f5f9;
        }

        ::-webkit-scrollbar-thumb {
            background: #cbd5e1;
            border-radius: 4px;
        }

        ::-webkit-scrollbar-thumb:hover {
            background: #94a3b8;
        }

        /* Card Hover Effects */
        .kpi-card {
            transition: transform 0.2s ease, box-shadow 0.2s ease;
        }

        .kpi-card:hover {
            transform: translateY(-2px);
            box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.1);
        }

        .activity-item {
            transition: background-color 0.2s ease;
        }

        .activity-item:hover {
            background-color: #f9fafb;
        }

        /* Table Styles */
        .product-table {
            border-collapse: separate;
            border-spacing: 0;
        }

        .product-table thead th {
            position: sticky;
            top: 0;
            background-color: #f8fafc;
            padding-top: 1rem;
            padding-bottom: 1rem;
            z-index: 20;
            font-weight: 600;
            letter-spacing: 0.025em;
        }

        .product-table thead::after {
            content: '';
            position: absolute;
            bottom: 0;
            left: 0;
            right: 0;
            height: 1px;
            background-color: #e5e7eb;
        }

        .product-table tbody tr {
            transition: background-color 0.15s ease;
        }

        .product-table tbody tr:hover {
            background-color: #f9fafb;
        }

        /* AI Card Styling */
        .ai-insight-card {
            background: white;
            border: 1px solid #e5e7eb;
            box-shadow: 0 1px 3px 0 rgba(0, 0, 0, 0.1);
        }
    </style>

    <!-- Load Tailwind CSS with defer -->
    <script src="https://cdn.tailwindcss.com" defer></script>

    <!-- Load Lucide icons with defer -->
    <script src="https://unpkg.com/lucide@latest/dist/umd/lucide.js" defer></script>

    <!-- Load Data Store -->
    <script src="data.js"></script>

    <!-- Additional performance hints -->
    <link rel="prefetch" href="login.html">
</head>

<body class="bg-gray-50 min-h-screen">
    <!-- Navigation Header -->
    <nav class="bg-white border-b border-gray-200">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
            <div class="flex justify-between items-center h-16">
                <div class="flex items-center space-x-4">
                    <!-- Mobile menu button -->
                    <button onclick="toggleMobileSidebar()"
                        class="lg:hidden inline-flex items-center justify-center p-2 rounded-lg text-gray-400 hover:text-gray-500 hover:bg-gray-100 focus:outline-none focus:ring-2 focus:ring-inset focus:ring-emerald-500"
                        title="Toggle mobile menu" aria-label="Toggle mobile menu">
                        <i data-lucide="menu" class="w-6 h-6"></i>
                    </button>

                    <div class="flex items-center space-x-3">
                        <div class="w-8 h-8 bg-emerald-500 rounded-lg flex items-center justify-center">
                            <i data-lucide="shield-check" class="w-5 h-5 text-white"></i>
                        </div>
                        <span class="text-lg font-semibold text-gray-900">Shirtify Admin</span>
                    </div>
                    <div class="hidden md:flex items-center space-x-1 text-sm text-gray-500">
                        <a href="dash.html" class="hover:text-gray-900 transition-colors">Home</a>
                        <i data-lucide="chevron-right" class="w-4 h-4"></i>
                        <span class="text-gray-900 font-medium">Dashboard</span>
                    </div>

                    <!-- Welcome message for new sessions -->
                    <div id="welcomeMessage"
                        class="hidden fixed top-20 left-1/2 transform -translate-x-1/2 bg-emerald-500 text-white px-6 py-3 rounded-lg shadow-lg z-50 animate-bounce">
                        <div class="flex items-center space-x-2">
                            <i data-lucide="check-circle" class="w-5 h-5"></i>
                            <span class="font-medium">Welcome back to Shirtify Admin!</span>
                        </div>
                    </div>
                </div>
                <div class="flex items-center space-x-4">
                    <!-- Global Search -->
                    <div class="relative hidden md:block">
                        <div class="relative">
                            <input type="text" id="globalSearch" placeholder="Search products, orders..."
                                class="w-64 pl-10 pr-4 py-2 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-cyan-500 focus:border-transparent text-sm">
                            <div class="absolute inset-y-0 left-0 pl-3 flex items-center pointer-events-none">
                                <i data-lucide="search" class="w-4 h-4 text-gray-400"></i>
                            </div>
                        </div>
                        <div id="searchResults"
                            class="absolute top-full left-0 right-0 bg-white border border-gray-200 rounded-lg shadow-lg mt-1 hidden z-50 max-h-80 overflow-y-auto">
                            <!-- Search results will appear here -->
                        </div>
                    </div>

                    <div id="userInfo" class="hidden flex items-center space-x-2 text-sm">
                        <div class="w-8 h-8 bg-emerald-100 rounded-full flex items-center justify-center">
                            <i data-lucide="user" class="w-4 h-4 text-emerald-600"></i>
                        </div>
                        <span class="text-gray-700">Welcome, <span id="userName" class="font-medium"></span></span>
                    </div>
                    <button onclick="logout()"
                        class="inline-flex items-center px-3 py-2 border border-gray-300 rounded-lg text-sm font-medium text-gray-700 bg-white hover:bg-gray-50 transition-colors">
                        <i data-lucide="log-out" class="w-4 h-4 mr-2"></i>
                        Logout
                    </button>
                </div>
            </div>
        </div>
    </nav>

    <main class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
        <div class="flex gap-8">
            <!-- Mobile Sidebar Overlay -->
            <div id="mobileSidebar" class="fixed inset-0 z-40 lg:hidden" style="display: none;">
                <div class="fixed inset-0 bg-gray-600 bg-opacity-75" onclick="toggleMobileSidebar()"></div>
                <div class="relative flex-1 flex flex-col max-w-xs w-full bg-white transform transition-transform duration-300 ease-in-out"
                    id="mobileSidebarContent">
                    <div class="absolute top-0 right-0 -mr-12 pt-2">
                        <button onclick="toggleMobileSidebar()"
                            class="ml-1 flex items-center justify-center h-10 w-10 rounded-full focus:outline-none focus:ring-2 focus:ring-inset focus:ring-white">
                            <i data-lucide="x" class="w-6 h-6 text-white"></i>
                        </button>
                    </div>
                    <div class="flex-1 h-0 pt-5 pb-4 overflow-y-auto">
                        <div class="flex-shrink-0 flex items-center px-4">
                            <h3 class="text-lg font-semibold text-gray-900">Navigation</h3>
                        </div>
                        <nav class="mt-5 px-2 space-y-1">
                            <button onclick="switchTab('overview'); toggleMobileSidebar();"
                                class="sidebar-nav-btn active w-full flex items-center space-x-3 px-4 py-3 text-left text-sm font-medium text-gray-700 hover:bg-gray-50 rounded-lg transition-colors">
                                <i data-lucide="layout-dashboard" class="w-5 h-5 text-emerald-500"></i>
                                <span>Dashboard Overview</span>
                            </button>
                            <button onclick="switchTab('products'); toggleMobileSidebar();"
                                class="sidebar-nav-btn w-full flex items-center space-x-3 px-4 py-3 text-left text-sm font-medium text-gray-700 hover:bg-gray-50 rounded-lg transition-colors">
                                <i data-lucide="package" class="w-5 h-5 text-blue-500"></i>
                                <span>Product Management</span>
                            </button>
                            <button onclick="switchTab('orders'); toggleMobileSidebar();"
                                class="sidebar-nav-btn w-full flex items-center space-x-3 px-4 py-3 text-left text-sm font-medium text-gray-700 hover:bg-gray-50 rounded-lg transition-colors">
                                <i data-lucide="shopping-cart" class="w-5 h-5 text-amber-500"></i>
                                <span>Order Management</span>
                            </button>
                            <button onclick="switchTab('analytics'); toggleMobileSidebar();"
                                class="sidebar-nav-btn w-full flex items-center space-x-3 px-4 py-3 text-left text-sm font-medium text-gray-700 hover:bg-gray-50 rounded-lg transition-colors">
                                <i data-lucide="trending-up" class="w-5 h-5 text-purple-500"></i>
                                <span>Analytics & AI</span>
                            </button>
                            <button onclick="switchTab('roles'); toggleMobileSidebar();"
                                class="sidebar-nav-btn w-full flex items-center space-x-3 px-4 py-3 text-left text-sm font-medium text-gray-700 hover:bg-gray-50 rounded-lg transition-colors">
                                <i data-lucide="users" class="w-5 h-5 text-cyan-500"></i>
                                <span>User Management</span>
                            </button>
                            <button onclick="switchTab('logs'); toggleMobileSidebar();"
                                class="sidebar-nav-btn w-full flex items-center space-x-3 px-4 py-3 text-left text-sm font-medium text-gray-700 hover:bg-gray-50 rounded-lg transition-colors">
                                <i data-lucide="clock" class="w-5 h-5 text-gray-500"></i>
                                <span>Activity Logs</span>
                            </button>
                        </nav>
                        <div class="mt-6 px-2">
                            <div class="border-t border-gray-200 pt-4">
                                <div class="space-y-2">
                                    <button onclick="openProductDrawer()"
                                        class="w-full flex items-center space-x-3 px-4 py-2 text-left text-sm font-medium text-emerald-700 hover:bg-emerald-50 rounded-lg transition-colors border border-emerald-200">
                                        <i data-lucide="plus" class="w-4 h-4"></i>
                                        <span>Add New Product</span>
                                    </button>
                                    <button onclick="switchTab('analytics')"
                                        class="w-full flex items-center space-x-3 px-4 py-2 text-left text-sm font-medium text-blue-700 hover:bg-blue-50 rounded-lg transition-colors border border-blue-200">
                                        <i data-lucide="trending-up" class="w-4 h-4"></i>
                                        <span>AI Predictions</span>
                                    </button>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>

            <!-- Desktop Sidebar Navigation -->
            <div class="hidden lg:block w-64 flex-shrink-0">
                <div class="bg-white rounded-xl shadow-sm p-6 sticky top-8">
                    <h3 class="text-lg font-semibold text-gray-900 mb-4">Quick Actions</h3>
                    <nav class="space-y-2">
                        <button onclick="switchTab('overview')"
                            class="sidebar-nav-btn active w-full flex items-center space-x-3 px-4 py-3 text-left text-sm font-medium text-gray-700 hover:bg-gray-50 rounded-lg transition-colors">
                            <i data-lucide="layout-dashboard" class="w-5 h-5 text-emerald-500"></i>
                            <span>Dashboard Overview</span>
                        </button>
                        <button onclick="switchTab('products')"
                            class="sidebar-nav-btn w-full flex items-center space-x-3 px-4 py-3 text-left text-sm font-medium text-gray-700 hover:bg-gray-50 rounded-lg transition-colors">
                            <i data-lucide="package" class="w-5 h-5 text-blue-500"></i>
                            <span>Product Management</span>
                        </button>
                        <button onclick="switchTab('orders')"
                            class="sidebar-nav-btn w-full flex items-center space-x-3 px-4 py-3 text-left text-sm font-medium text-gray-700 hover:bg-gray-50 rounded-lg transition-colors">
                            <i data-lucide="shopping-cart" class="w-5 h-5 text-amber-500"></i>
                            <span>Order Management</span>
                        </button>
                        <button onclick="switchTab('analytics')"
                            class="sidebar-nav-btn w-full flex items-center space-x-3 px-4 py-3 text-left text-sm font-medium text-gray-700 hover:bg-gray-50 rounded-lg transition-colors">
                            <i data-lucide="trending-up" class="w-5 h-5 text-purple-500"></i>
                            <span>Analytics & AI</span>
                        </button>
                        <button onclick="switchTab('roles')"
                            class="sidebar-nav-btn w-full flex items-center space-x-3 px-4 py-3 text-left text-sm font-medium text-gray-700 hover:bg-gray-50 rounded-lg transition-colors">
                            <i data-lucide="users" class="w-5 h-5 text-cyan-500"></i>
                            <span>User Management</span>
                        </button>
                        <button onclick="switchTab('logs')"
                            class="sidebar-nav-btn w-full flex items-center space-x-3 px-4 py-3 text-left text-sm font-medium text-gray-700 hover:bg-gray-50 rounded-lg transition-colors">
                            <i data-lucide="clock" class="w-5 h-5 text-gray-500"></i>
                            <span>Activity Logs</span>
                        </button>
                    </nav>

                    <div class="border-t border-gray-200 mt-6 pt-6">
                        <h4 class="text-sm font-semibold text-gray-900 mb-3">Direct Actions</h4>
                        <div class="space-y-2">
                            <button onclick="openProductDrawer()"
                                class="w-full flex items-center space-x-3 px-4 py-2 text-left text-sm font-medium text-emerald-700 hover:bg-emerald-50 rounded-lg transition-colors border border-emerald-200">
                                <i data-lucide="plus" class="w-4 h-4"></i>
                                <span>Add New Product</span>
                            </button>
                            <a href="predict.html"
                                class="w-full flex items-center space-x-3 px-4 py-2 text-left text-sm font-medium text-blue-700 hover:bg-blue-50 rounded-lg transition-colors border border-blue-200">
                                <i data-lucide="trending-up" class="w-4 h-4"></i>
                                <span>AI Predictions</span>
                            </a>
                        </div>
                    </div>
                </div>
            </div>

            <!-- Main Content -->
            <div class="flex-1 min-w-0 flex flex-col">
                <!-- Page Header -->
                <div
                    class="mb-10 flex flex-col md:flex-row md:items-end md:justify-between gap-4 border-b border-gray-100 pb-6">
                    <div>
                        <h1 class="text-2xl font-bold text-gray-900 tracking-tight mb-1">Admin Dashboard</h1>
                        <p class="text-gray-500 text-sm font-medium">Manage your Store, Products, and Analysis</p>
                    </div>
                    <div class="flex items-center space-x-3">
                        <span
                            class="inline-flex items-center px-3 py-1.5 rounded-md text-sm font-medium bg-white text-gray-600 border border-gray-200 shadow-sm">
                            <i data-lucide="calendar" class="w-4 h-4 mr-2 text-gray-400"></i>
                            <span id="currentDate">Today</span>
                        </span>
                    </div>
                </div>

                <!-- Tab Navigation -->
                <div class="bg-white rounded-xl shadow-sm border border-gray-100 mb-8 p-1">
                    <div class="flex flex-wrap items-center gap-1 p-1">
                        <button onclick="switchTab('overview')"
                            class="tab-btn flex-1 min-w-[100px] px-4 py-2.5 text-sm font-medium rounded-lg transition-all duration-200 text-white bg-gray-900 shadow-sm">Overview</button>
                        <button onclick="switchTab('products')"
                            class="tab-btn flex-1 min-w-[100px] px-4 py-2.5 text-sm font-medium text-gray-600 hover:text-gray-900 hover:bg-gray-50 rounded-lg transition-all duration-200">Products</button>
                        <button onclick="switchTab('orders')"
                            class="tab-btn flex-1 min-w-[100px] px-4 py-2.5 text-sm font-medium text-gray-600 hover:text-gray-900 hover:bg-gray-50 rounded-lg transition-all duration-200">Orders</button>
                        <button onclick="switchTab('analytics')"
                            class="tab-btn flex-1 min-w-[100px] px-4 py-2.5 text-sm font-medium text-gray-600 hover:text-gray-900 hover:bg-gray-50 rounded-lg transition-all duration-200">Analytics</button>
                        <button onclick="switchTab('roles')"
                            class="tab-btn flex-1 min-w-[100px] px-4 py-2.5 text-sm font-medium text-gray-600 hover:text-gray-900 hover:bg-gray-50 rounded-lg transition-all duration-200">Roles</button>
                        <button onclick="switchTab('logs')"
                            class="tab-btn flex-1 min-w-[100px] px-4 py-2.5 text-sm font-medium text-gray-600 hover:text-gray-900 hover:bg-gray-50 rounded-lg transition-all duration-200">Logs</button>
                    </div>
                </div>

                <!-- Overview Tab -->
                <div id="overview-tab" class="tab-content active">
                    <div class="space-y-6">
                        <!-- KPI Cards -->
                        <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-6">
                            <div class="kpi-card bg-white rounded-xl shadow-sm p-6 border border-gray-100">
                                <div class="flex items-center justify-between mb-4">
                                    <div class="w-12 h-12 bg-emerald-100 rounded-lg flex items-center justify-center">
                                        <i data-lucide="dollar-sign" class="w-6 h-6 text-emerald-600"></i>
                                    </div>
                                    <span
                                        class="text-xs font-medium text-emerald-600 bg-emerald-50 px-2 py-1 rounded-full">+12.5%</span>
                                </div>
                                <h3 class="text-sm font-medium text-gray-600 mb-1">Total Sales</h3>
                                <p class="text-2xl font-bold text-gray-900" id="totalSales">Rs 0</p>
                                <div class="mt-3">
                                    <canvas id="salesSparkline" width="100" height="30"></canvas>
                                </div>
                                <p class="text-xs text-gray-500 mt-2">Last 30 days</p>
                            </div>

                            <div class="kpi-card bg-white rounded-xl shadow-sm p-6 border border-gray-100">
                                <div class="flex items-center justify-between mb-4">
                                    <div class="w-12 h-12 bg-blue-100 rounded-lg flex items-center justify-center">
                                        <i data-lucide="shopping-cart" class="w-6 h-6 text-blue-600"></i>
                                    </div>
                                    <span
                                        class="text-xs font-medium text-blue-600 bg-blue-50 px-2 py-1 rounded-full">+12.5%</span>
                                </div>
                                <h3 class="text-sm font-medium text-gray-600 mb-1">Total Orders</h3>
                                <p class="text-2xl font-bold text-gray-900" id="totalOrders">0</p>
                                <div class="mt-3">
                                    <canvas id="ordersSparkline" width="100" height="30"></canvas>
                                </div>
                                <p class="text-xs text-gray-500 mt-2">Last 30 days</p>
                            </div>

                            <div class="kpi-card bg-white rounded-xl shadow-sm p-6 border border-gray-100">
                                <div class="flex items-center justify-between mb-4">
                                    <div class="w-12 h-12 bg-amber-100 rounded-lg flex items-center justify-center">
                                        <i data-lucide="package" class="w-6 h-6 text-amber-600"></i>
                                    </div>
                                    <span
                                        class="text-xs font-medium text-amber-600 bg-amber-50 px-2 py-1 rounded-full">+3</span>
                                </div>
                                <h3 class="text-sm font-medium text-gray-600 mb-1">Products</h3>
                                <p class="text-2xl font-bold text-gray-900" id="totalProducts">0</p>
                                <div class="mt-3">
                                    <canvas id="productsSparkline" width="100" height="30"></canvas>
                                </div>
                                <p class="text-xs text-gray-500 mt-2">Active designs</p>
                            </div>

                            <div class="kpi-card bg-white rounded-xl shadow-sm p-6 border border-gray-100">
                                <div class="flex items-center justify-between mb-4">
                                    <div class="w-12 h-12 bg-purple-100 rounded-lg flex items-center justify-center">
                                        <i data-lucide="users" class="w-6 h-6 text-purple-600"></i>
                                    </div>
                                    <span
                                        class="text-xs font-medium text-purple-600 bg-purple-50 px-2 py-1 rounded-full">+18.7%</span>
                                </div>
                                <h3 class="text-sm font-medium text-gray-600 mb-1">Customers</h3>
                                <p class="text-2xl font-bold text-gray-900" id="totalVisitors">0</p>
                                <div class="mt-3">
                                    <canvas id="customersSparkline" width="100" height="30"></canvas>
                                </div>
                                <p class="text-xs text-gray-500 mt-2">Total registered</p>
                            </div>
                        </div>

                        <!-- Quick Actions -->
                        <div class="bg-white rounded-xl shadow-sm p-6 mb-6">
                            <div class="flex items-center justify-between mb-6">
                                <div class="flex items-center space-x-3">
                                    <div class="w-10 h-10 bg-emerald-100 rounded-lg flex items-center justify-center">
                                        <i data-lucide="zap" class="w-5 h-5 text-emerald-600"></i>
                                    </div>
                                    <div>
                                        <h2 class="text-xl font-semibold text-gray-900 tracking-tight">Quick Actions
                                        </h2>
                                        <p class="text-sm text-gray-500">Common tasks and shortcuts</p>
                                    </div>
                                </div>
                            </div>
                            <div class="grid grid-cols-2 md:grid-cols-4 gap-4">
                                <button onclick="openProductDrawer()"
                                    class="flex flex-col items-center p-4 bg-emerald-50 hover:bg-emerald-100 rounded-lg transition-all duration-200 hover:-translate-y-1 hover:shadow-md group">
                                    <div
                                        class="w-12 h-12 bg-emerald-500 rounded-lg flex items-center justify-center mb-3 group-hover:scale-110 transition-transform">
                                        <i data-lucide="plus" class="w-6 h-6 text-white"></i>
                                    </div>
                                    <span class="text-sm font-medium text-emerald-700">Add Product</span>
                                </button>
                                <button onclick="switchTab('orders')"
                                    class="flex flex-col items-center p-4 bg-blue-50 hover:bg-blue-100 rounded-lg transition-all duration-200 hover:-translate-y-1 hover:shadow-md group">
                                    <div
                                        class="w-12 h-12 bg-blue-500 rounded-lg flex items-center justify-center mb-3 group-hover:scale-110 transition-transform">
                                        <i data-lucide="shopping-cart" class="w-6 h-6 text-white"></i>
                                    </div>
                                    <span class="text-sm font-medium text-blue-700">Manage Orders</span>
                                </button>
                                <button onclick="switchTab('analytics')"
                                    class="flex flex-col items-center p-4 bg-purple-50 hover:bg-purple-100 rounded-lg transition-all duration-200 hover:-translate-y-1 hover:shadow-md group">
                                    <div
                                        class="w-12 h-12 bg-purple-500 rounded-lg flex items-center justify-center mb-3 group-hover:scale-110 transition-transform">
                                        <i data-lucide="trending-up" class="w-6 h-6 text-white"></i>
                                    </div>
                                    <span class="text-sm font-medium text-purple-700">AI Predictions</span>
                                </button>
                                <button onclick="switchTab('analytics')"
                                    class="flex flex-col items-center p-4 bg-amber-50 hover:bg-amber-100 rounded-lg transition-all duration-200 hover:-translate-y-1 hover:shadow-md group">
                                    <div
                                        class="w-12 h-12 bg-amber-500 rounded-lg flex items-center justify-center mb-3 group-hover:scale-110 transition-transform">
                                        <i data-lucide="bar-chart-3" class="w-6 h-6 text-white"></i>
                                    </div>
                                    <span class="text-sm font-medium text-amber-700">Analytics</span>
                                </button>
                            </div>
                        </div>

                        <!-- Recent Activity -->
                        <div class="bg-white rounded-xl shadow-sm p-6">
                            <div class="flex items-center justify-between mb-6">
                                <div class="flex items-center space-x-3">
                                    <div class="w-10 h-10 bg-cyan-100 rounded-lg flex items-center justify-center">
                                        <i data-lucide="activity" class="w-5 h-5 text-cyan-600"></i>
                                    </div>
                                    <div>
                                        <h2 class="text-xl font-semibold text-gray-900 tracking-tight">Recent Activity
                                        </h2>
                                        <p class="text-sm text-gray-500">Latest updates and events</p>
                                    </div>
                                </div>
                            </div>
                            <div class="space-y-3" id="recentActivity">
                                <div
                                    class="activity-item flex items-start space-x-4 p-4 bg-gray-50 rounded-lg border border-gray-100">
                                    <div
                                        class="w-8 h-8 bg-emerald-100 rounded-full flex items-center justify-center flex-shrink-0">
                                        <i data-lucide="shopping-bag" class="w-4 h-4 text-emerald-600"></i>
                                    </div>
                                    <div class="flex-1 min-w-0">
                                        <p class="text-sm font-medium text-gray-900">New order #ORD-7783 received</p>
                                        <p class="text-xs text-gray-500 mt-1">Customer: Ahmed Raza (Karachi) • Amount:
                                            Rs 3,500</p>
                                        <p class="text-xs text-gray-400 mt-1">2 minutes ago</p>
                                    </div>
                                </div>
                                <div
                                    class="activity-item flex items-start space-x-4 p-4 bg-gray-50 rounded-lg border border-gray-100">
                                    <div
                                        class="w-8 h-8 bg-blue-100 rounded-full flex items-center justify-center flex-shrink-0">
                                        <i data-lucide="package" class="w-4 h-4 text-blue-600"></i>
                                    </div>
                                    <div class="flex-1 min-w-0">
                                        <p class="text-sm font-medium text-gray-900">Product "Urban Streetwear Graphic"
                                            updated</p>
                                        <p class="text-xs text-gray-500 mt-1">Stock replenished • New sizes added (XXL)
                                        </p>
                                        <p class="text-xs text-gray-400 mt-1">15 minutes ago</p>
                                    </div>
                                </div>
                                <div
                                    class="activity-item flex items-start space-x-4 p-4 bg-gray-50 rounded-lg border border-gray-100">
                                    <div
                                        class="w-8 h-8 bg-amber-100 rounded-full flex items-center justify-center flex-shrink-0">
                                        <i data-lucide="user-plus" class="w-4 h-4 text-amber-600"></i>
                                    </div>
                                    <div class="flex-1 min-w-0">
                                        <p class="text-sm font-medium text-gray-900">New customer registered</p>
                                        <p class="text-xs text-gray-500 mt-1">Fatima Sheikh • Lahore • WhatsApp: +92 300
                                            1234567</p>
                                        <p class="text-xs text-gray-400 mt-1">1 hour ago</p>
                                    </div>
                                </div>
                                <div
                                    class="activity-item flex items-start space-x-4 p-4 bg-gray-50 rounded-lg border border-gray-100">
                                    <div
                                        class="w-8 h-8 bg-purple-100 rounded-full flex items-center justify-center flex-shrink-0">
                                        <i data-lucide="trending-up" class="w-4 h-4 text-purple-600"></i>
                                    </div>
                                    <div class="flex-1 min-w-0">
                                        <p class="text-sm font-medium text-gray-900">Weekly sales report generated</p>
                                        <p class="text-xs text-gray-500 mt-1">Total: Rs 1,24,500 • Top seller: Heavy Tee
                                            (Black)</p>
                                        <p class="text-xs text-gray-400 mt-1">2 hours ago</p>
                                    </div>
                                </div>
                                <div
                                    class="activity-item flex items-start space-x-4 p-4 bg-gray-50 rounded-lg border border-gray-100">
                                    <div
                                        class="w-8 h-8 bg-red-100 rounded-full flex items-center justify-center flex-shrink-0">
                                        <i data-lucide="alert-circle" class="w-4 h-4 text-red-600"></i>
                                    </div>
                                    <div class="flex-1 min-w-0">
                                        <p class="text-sm font-medium text-gray-900">Low stock alert</p>
                                        <p class="text-xs text-gray-500 mt-1">Premium Blank Tee (Olive, M) - Only 3
                                            units remaining</p>
                                        <p class="text-xs text-gray-400 mt-1">3 hours ago</p>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>

                <!-- Products Tab -->
                <div id="products-tab" class="tab-content">
                    <div class="bg-white rounded-xl shadow-sm p-6">
                        <div class="flex flex-col sm:flex-row sm:items-center sm:justify-between mb-6">
                            <div class="flex items-center space-x-3 mb-4 sm:mb-0">
                                <div class="w-10 h-10 bg-amber-100 rounded-lg flex items-center justify-center">
                                    <i data-lucide="package" class="w-5 h-5 text-amber-600"></i>
                                </div>
                                <div>
                                    <h2 class="text-xl font-semibold text-gray-900 tracking-tight">Product Management
                                    </h2>
                                    <p class="text-sm text-gray-500">Manage your Store Products</p>
                                </div>
                            </div>
                            <button onclick="openProductDrawer()"
                                class="inline-flex items-center px-4 py-2 bg-emerald-500 hover:bg-emerald-600 text-white text-sm font-medium rounded-full transition-colors">
                                <i data-lucide="plus" class="w-4 h-4 mr-2"></i>
                                Add Product
                            </button>
                        </div>

                        <!-- Filter Bar -->
                        <div class="bg-gray-50 rounded-lg p-4 mb-6 border border-gray-200">
                            <div class="grid grid-cols-1 md:grid-cols-3 gap-4">
                                <div>
                                    <label class="block text-xs font-medium text-gray-700 mb-2">Filter by Color</label>
                                    <select id="filterColor" onchange="filterProducts()"
                                        class="w-full px-3 py-2 text-sm border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-cyan-500 bg-white cursor-pointer">
                                        <option value="">All Colors</option>
                                        <option value="Black">Black</option>
                                        <option value="White">White</option>
                                        <option value="Olive">Olive</option>
                                        <option value="Green">Green</option>
                                    </select>
                                </div>
                                <div>
                                    <label class="block text-xs font-medium text-gray-700 mb-2">Filter by Size</label>
                                    <select id="filterSize" onchange="filterProducts()"
                                        class="w-full px-3 py-2 text-sm border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-cyan-500 bg-white cursor-pointer">
                                        <option value="">All Sizes</option>
                                        <option value="S">Small (S)</option>
                                        <option value="M">Medium (M)</option>
                                        <option value="L">Large (L)</option>
                                        <option value="XL">Extra Large (XL)</option>
                                    </select>
                                </div>
                                <div>
                                    <label class="block text-xs font-medium text-gray-700 mb-2">Price Range (Rs)</label>
                                    <div class="flex items-center space-x-2">
                                        <input type="number" id="priceMin" placeholder="Min" onchange="filterProducts()"
                                            class="w-full px-3 py-2 text-sm border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-cyan-500 bg-white">
                                        <span class="text-gray-500">-</span>
                                        <input type="number" id="priceMax" placeholder="Max" onchange="filterProducts()"
                                            class="w-full px-3 py-2 text-sm border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-cyan-500 bg-white">
                                    </div>
                                </div>
                            </div>
                        </div>

                        <div class="overflow-x-auto">
                            <table class="product-table min-w-full divide-y divide-gray-200">
                                <thead class="bg-gray-50 sticky top-0 z-10">
                                    <tr>
                                        <th
                                            class="px-6 py-3 text-left text-xs font-semibold tracking-wider text-gray-500 uppercase">
                                            Thumbnail</th>
                                        <th
                                            class="px-6 py-3 text-left text-xs font-semibold tracking-wider text-gray-500 uppercase">
                                            Design Info</th>
                                        <th
                                            class="px-6 py-3 text-left text-xs font-semibold tracking-wider text-gray-500 uppercase">
                                            Base</th>
                                        <th
                                            class="px-6 py-3 text-left text-xs font-semibold tracking-wider text-gray-500 uppercase">
                                            Sizes/Variants</th>
                                        <th
                                            class="px-6 py-3 text-left text-xs font-semibold tracking-wider text-gray-500 uppercase">
                                            Price</th>
                                        <th
                                            class="px-6 py-3 text-left text-xs font-semibold tracking-wider text-gray-500 uppercase">
                                            Stock Status</th>
                                        <th
                                            class="px-6 py-3 text-left text-xs font-semibold tracking-wider text-gray-500 uppercase">
                                            Actions</th>
                                    </tr>
                                </thead>
                                <tbody class="bg-white divide-y divide-gray-200" id="productsTableBody">
                                    <!-- Products will be populated by JavaScript -->
                                </tbody>
                            </table>
                            <div id="productsEmptyState" class="hidden text-center py-12">
                                <i data-lucide="package" class="w-16 h-16 text-gray-300 mx-auto mb-4"></i>
                                <p class="text-gray-500 text-lg font-medium">No products found</p>
                                <p class="text-gray-400 text-sm mt-2">Add your first product to get started</p>
                                <button onclick="openProductDrawer()"
                                    class="mt-4 inline-flex items-center px-4 py-2 bg-emerald-500 hover:bg-emerald-600 text-white text-sm font-medium rounded-full transition-colors">
                                    <i data-lucide="plus" class="w-4 h-4 mr-2"></i>
                                    Add Product
                                </button>
                            </div>
                        </div>
                    </div>
                </div>

                <div id="orders-tab" class="tab-content">
                    <div class="bg-white rounded-xl shadow-sm p-6">
                        <div class="flex items-center space-x-3 mb-6">
                            <div class="w-10 h-10 bg-amber-100 rounded-lg flex items-center justify-center">
                                <i data-lucide="package-open" class="w-5 h-5 text-amber-600"></i>
                            </div>
                            <div>
                                <h2 class="text-xl font-semibold text-gray-900 tracking-tight">Order Management</h2>
                                <p class="text-sm text-gray-500">View and manage customer orders</p>
                            </div>
                        </div>

                        <!-- Search and Filter Bar -->
                        <div class="bg-gray-50 rounded-lg p-4 mb-6 border border-gray-200">
                            <div class="grid grid-cols-1 md:grid-cols-3 gap-4">
                                <div>
                                    <label class="block text-xs font-medium text-gray-700 mb-2">Search Orders</label>
                                    <input type="text" id="orderSearch" placeholder="Search by order ID, customer..."
                                        onkeyup="filterOrders()"
                                        class="w-full px-3 py-2 text-sm border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-cyan-500 bg-white">
                                </div>
                                <div>
                                    <label class="block text-xs font-medium text-gray-700 mb-2">Filter by Status</label>
                                    <select id="statusFilter" onchange="filterOrders()"
                                        class="w-full px-3 py-2 text-sm border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-cyan-500 bg-white cursor-pointer">
                                        <option value="">All Statuses</option>
                                        <option value="Pending">Pending</option>
                                        <option value="Processing">Processing</option>
                                        <option value="Shipped">Shipped</option>
                                        <option value="Delivered">Delivered</option>
                                    </select>
                                </div>
                                <div>
                                    <label class="block text-xs font-medium text-gray-700 mb-2">Sort by</label>
                                    <select id="sortOrders" onchange="filterOrders()"
                                        class="w-full px-3 py-2 text-sm border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-cyan-500 bg-white cursor-pointer">
                                        <option value="date-desc">Newest First</option>
                                        <option value="date-asc">Oldest First</option>
                                        <option value="amount-desc">Highest Amount</option>
                                        <option value="amount-asc">Lowest Amount</option>
                                    </select>
                                </div>
                            </div>
                        </div>

                        <!-- Pipeline Summary -->
                        <div class="grid grid-cols-2 md:grid-cols-4 gap-4 mb-6">
                            <div class="bg-amber-50 border border-amber-200 rounded-lg p-4">
                                <p class="text-xs font-medium text-amber-700 mb-1">Pending</p>
                                <p class="text-2xl font-bold text-amber-900" id="pendingOrders">0</p>
                            </div>
                            <div class="bg-blue-50 border border-blue-200 rounded-lg p-4">
                                <p class="text-xs font-medium text-blue-700 mb-1">Processing</p>
                                <p class="text-2xl font-bold text-blue-900" id="processingOrders">0</p>
                            </div>
                            <div class="bg-purple-50 border border-purple-200 rounded-lg p-4">
                                <p class="text-xs font-medium text-purple-700 mb-1">Shipped</p>
                                <p class="text-2xl font-bold text-purple-900" id="shippedOrders">0</p>
                            </div>
                            <div class="bg-emerald-50 border border-emerald-200 rounded-lg p-4">
                                <p class="text-xs font-medium text-emerald-700 mb-1">Delivered</p>
                                <p class="text-2xl font-bold text-emerald-900" id="deliveredOrders">0</p>
                            </div>
                        </div>

                        <!-- Orders Table -->
                        <div class="overflow-x-auto">
                            <table class="product-table min-w-full divide-y divide-gray-200">
                                <thead class="bg-gray-50 sticky top-0 z-10">
                                    <tr>
                                        <th
                                            class="px-6 py-3 text-left text-xs font-semibold tracking-wider text-gray-500 uppercase">
                                            Order ID</th>
                                        <th
                                            class="px-6 py-3 text-left text-xs font-semibold tracking-wider text-gray-500 uppercase">
                                            Customer</th>
                                        <th
                                            class="px-6 py-3 text-left text-xs font-semibold tracking-wider text-gray-500 uppercase">
                                            Ordered Items</th>
                                        <th
                                            class="px-6 py-3 text-left text-xs font-semibold tracking-wider text-gray-500 uppercase">
                                            Amount (Rs)</th>
                                        <th
                                            class="px-6 py-3 text-left text-xs font-semibold tracking-wider text-gray-500 uppercase">
                                            Payment</th>
                                        <th
                                            class="px-6 py-3 text-left text-xs font-semibold tracking-wider text-gray-500 uppercase">
                                            Status</th>
                                        <th
                                            class="px-6 py-3 text-left text-xs font-semibold tracking-wider text-gray-500 uppercase">
                                            Actions</th>
                                    </tr>
                                </thead>
                                <tbody class="bg-white divide-y divide-gray-200" id="ordersTableBody">
                                    <!-- Orders will be populated by JavaScript -->
                                </tbody>
                            </table>
                        </div>
                    </div>
                </div>

                <div id="analytics-tab" class="tab-content">
                    <!-- Sales Analytics Section (NEW Feature 1) -->
                    <div class="bg-white rounded-2xl shadow-sm p-8 mb-8 border border-gray-100">
                        <div class="flex flex-col md:flex-row md:items-center justify-between mb-8 gap-4">
                            <div>
                                <h1 class="text-2xl font-bold text-gray-900 tracking-tight mb-1">Sales Analytics</h1>
                                <p class="text-sm font-medium text-gray-500">Real-time performance across designs and sizes</p>
                            </div>
                            
                            <!-- Date Filters -->
                            <div class="flex flex-wrap items-end gap-3 bg-gray-50 p-4 rounded-xl border border-gray-200">
                                <div>
                                    <label class="block text-xs font-semibold text-gray-600 mb-1.5 uppercase tracking-wider">Start Date</label>
                                    <input type="date" id="salesStartDate" class="px-3 py-2 text-sm border border-gray-300 rounded-lg focus:ring-2 focus:ring-emerald-500 outline-none">
                                </div>
                                <div>
                                    <label class="block text-xs font-semibold text-gray-600 mb-1.5 uppercase tracking-wider">End Date</label>
                                    <input type="date" id="salesEndDate" class="px-3 py-2 text-sm border border-gray-300 rounded-lg focus:ring-2 focus:ring-emerald-500 outline-none">
                                </div>
                                <div>
                                    <label class="block text-xs font-semibold text-gray-600 mb-1.5 uppercase tracking-wider">Interval</label>
                                    <select id="salesInterval" class="px-3 py-2 text-sm border border-gray-300 rounded-lg focus:ring-2 focus:ring-emerald-500 outline-none cursor-pointer">
                                        <option value="daily">Daily</option>
                                        <option value="weekly">Weekly</option>
                                    </select>
                                </div>
                                <button onclick="updateSalesAnalytics()" class="px-4 py-2 bg-emerald-600 hover:bg-emerald-700 text-white text-sm font-bold rounded-lg transition-all flex items-center shadow-sm">
                                    <i data-lucide="refresh-cw" class="w-4 h-4 mr-2"></i>
                                    Refresh
                                </button>
                            </div>
                        </div>

                        <!-- Charts Grid -->
                        <div class="grid grid-cols-1 lg:grid-cols-3 gap-8">
                            <!-- Main Trend Chart -->
                            <div class="lg:col-span-2 bg-white p-6 rounded-xl border border-gray-100 shadow-sm">
                                <div class="flex items-center justify-between mb-6">
                                    <h3 class="font-bold text-gray-900">Order Trends Over Time</h3>
                                    <div class="flex items-center space-x-2">
                                        <span class="w-3 h-3 bg-emerald-500 rounded-full"></span>
                                        <span class="text-xs font-medium text-gray-500">Orders & Revenue</span>
                                    </div>
                                </div>
                                <div class="h-[350px]">
                                    <canvas id="salesTrendsChart"></canvas>
                                </div>
                            </div>

                            <!-- Size Distribution -->
                            <div class="bg-white p-6 rounded-xl border border-gray-100 shadow-sm">
                                <h3 class="font-bold text-gray-900 mb-6">Size Popularity (S–XL)</h3>
                                <div class="h-[300px] flex items-center justify-center">
                                    <canvas id="salesSizeChart"></canvas>
                                </div>
                                <div id="sizeLegend" class="mt-4 grid grid-cols-2 gap-2 text-xs"></div>
                            </div>
                        </div>

                        <!-- Top Designs Bar Chart -->
                        <div class="mt-8 bg-white p-6 rounded-xl border border-gray-100 shadow-sm">
                            <h3 class="font-bold text-gray-900 mb-6">Top-Selling T-Shirt Designs</h3>
                            <div class="h-[300px]">
                                <canvas id="topDesignsChart"></canvas>
                            </div>
                        </div>
                    </div>

                    <div class="bg-white rounded-2xl shadow-sm p-8">
                        <h1 class="text-3xl font-bold text-emerald-800 mb-8">Predictive Analysis And Forecasting</h1>

                        <div class="mb-6">
                            <h2 class="text-xl font-bold text-gray-900 mb-4">Forecast Results</h2>
                            <div class="mb-4">
                                <label for="timePeriod" class="block text-sm font-medium text-gray-700 mb-2">Select Time
                                    Period</label>
                                <select id="timePeriod"
                                    class="px-4 py-2 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-cyan-500 cursor-pointer">
                                    <option value="3">Last 3 Months</option>
                                    <option value="6" selected>Last 6 Months</option>
                                    <option value="12">Last 12 Months</option>
                                </select>
                            </div>
                        </div>

                        <div class="mb-8">
                            <h3 class="text-base font-medium text-gray-700 mb-4">Forecast Graph</h3>
                            <div class="bg-white p-4 rounded-lg border border-gray-200">
                                <canvas id="forecastChart"></canvas>
                            </div>
                        </div>

                        <div class="grid grid-cols-1 md:grid-cols-3 gap-4 mb-6">
                            <div class="bg-blue-50 rounded-xl p-6 border border-blue-100">
                                <div class="flex items-center space-x-3">
                                    <div class="w-10 h-10 bg-blue-100 rounded-lg flex items-center justify-center">
                                        <i data-lucide="trending-up" class="w-5 h-5 text-blue-600"></i>
                                    </div>
                                    <div>
                                        <p class="text-sm text-gray-600 font-medium">Predicted Units (Next 4 Months)</p>
                                        <p class="text-2xl font-bold text-gray-900" id="predictUnits">1000</p>
                                    </div>
                                </div>
                            </div>

                            <div class="bg-emerald-50 rounded-xl p-6 border border-emerald-100">
                                <div class="flex items-center space-x-3">
                                    <div class="w-10 h-10 bg-emerald-100 rounded-lg flex items-center justify-center">
                                        <i data-lucide="alert-triangle" class="w-5 h-5 text-emerald-600"></i>
                                    </div>
                                    <div>
                                        <p class="text-sm text-gray-600 font-medium">Forecast Accuracy</p>
                                        <p class="text-2xl font-bold text-gray-900" id="errorRate">96.5%</p>
                                    </div>
                                </div>
                            </div>

                            <div class="bg-amber-50 rounded-xl p-6 border border-amber-100">
                                <div class="flex items-center space-x-3">
                                    <div class="w-10 h-10 bg-amber-100 rounded-lg flex items-center justify-center">
                                        <i data-lucide="dollar-sign" class="w-5 h-5 text-amber-600"></i>
                                    </div>
                                    <div>
                                        <p class="text-sm text-gray-600 font-medium">Expected Revenue Forecast</p>
                                        <p class="text-2xl font-bold text-gray-900" id="revenueForecast">Rs 6,346,563
                                        </p>
                                    </div>
                                </div>
                            </div>
                        </div>

                        <!-- Category Analysis Section -->
                        <div class="mb-8">
                            <h2 class="text-xl font-bold text-gray-900 mb-4">Category & Size Analysis</h2>
                            <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
                                <!-- Category Chart -->
                                <div class="bg-white p-4 rounded-lg border border-gray-200">
                                    <h3 class="text-base font-semibold text-gray-700 mb-3">Sales by Category</h3>
                                    <canvas id="categoryChart" style="max-height: 300px;"></canvas>
                                </div>

                                <!-- Size Chart -->
                                <div class="bg-white p-4 rounded-lg border border-gray-200">
                                    <h3 class="text-base font-semibold text-gray-700 mb-3">Sales by Size</h3>
                                    <canvas id="sizeChart" style="max-height: 300px;"></canvas>
                                </div>
                            </div>
                        </div>

                        <!-- Top Products Section -->
                        <div class="mb-8">
                            <h2 class="text-xl font-bold text-gray-900 mb-4">Top Selling Products</h2>
                            <div
                                class="bg-gradient-to-r from-purple-50 to-pink-50 rounded-xl p-6 border border-purple-100">
                                <div class="grid grid-cols-1 md:grid-cols-3 gap-4" id="topProducts">
                                    <!-- Will be populated by JavaScript -->
                                </div>
                            </div>
                        </div>

                        <!-- Detailed Sales Table -->
                        <div class="mb-8">
                            <h2 class="text-xl font-bold text-gray-900 mb-4">Product Performance Details</h2>
                            <div class="overflow-x-auto bg-white rounded-lg border border-gray-200">
                                <table class="min-w-full divide-y divide-gray-200">
                                    <thead class="bg-gray-50 sticky top-0 z-10">
                                        <tr>
                                            <th
                                                class="px-6 py-3 text-left text-xs font-semibold tracking-wider text-gray-500 uppercase">
                                                Product Name</th>
                                            <th
                                                class="px-6 py-3 text-left text-xs font-semibold tracking-wider text-gray-500 uppercase">
                                                Category</th>
                                            <th
                                                class="px-6 py-3 text-left text-xs font-semibold tracking-wider text-gray-500 uppercase">
                                                Size</th>
                                            <th
                                                class="px-6 py-3 text-left text-xs font-semibold tracking-wider text-gray-500 uppercase">
                                                Color</th>
                                            <th
                                                class="px-6 py-3 text-left text-xs font-semibold tracking-wider text-gray-500 uppercase">
                                                Units Sold</th>
                                            <th
                                                class="px-6 py-3 text-left text-xs font-semibold tracking-wider text-gray-500 uppercase">
                                                Revenue</th>
                                            <th
                                                class="px-6 py-3 text-left text-xs font-semibold tracking-wider text-gray-500 uppercase">
                                                Trend</th>
                                        </tr>
                                    </thead>
                                    <tbody class="bg-white divide-y divide-gray-200" id="productTable">
                                        <!-- Will be populated by JavaScript -->
                                    </tbody>
                                </table>
                            </div>
                        </div>

                        <div class="flex space-x-4">
                            <button onclick="generateForecast()"
                                class="px-6 py-3 bg-emerald-600 hover:bg-emerald-700 text-white font-medium rounded-lg transition-colors">
                                Generate New Forecast
                            </button>
                            <button onclick="exportChart()"
                                class="px-6 py-3 bg-white border-2 border-gray-300 hover:bg-gray-50 text-gray-700 font-medium rounded-lg transition-colors">
                                Export Chart/PDF
                            </button>
                        </div>

                        <!-- Dataset Info Panel -->
                        <div
                            class="mt-8 bg-gradient-to-r from-blue-50 to-purple-50 rounded-xl p-6 border border-blue-100">
                            <h3 class="text-lg font-semibold text-gray-900 mb-3">📊 Demo Dataset Information</h3>
                            <div class="grid grid-cols-1 md:grid-cols-2 gap-4 text-sm">
                                <div>
                                    <p class="text-gray-700"><span class="font-semibold">Data Period:</span> <span
                                            id="dataPeriod">Oct 2023 - Sep 2024</span></p>
                                    <p class="text-gray-700"><span class="font-semibold">Total Records:</span> <span
                                            id="totalRecords">12 months</span></p>
                                    <p class="text-gray-700"><span class="font-semibold">Average Units/Month:</span>
                                        <span id="avgUnits">523 units</span>
                                    </p>
                                </div>
                                <div>
                                    <p class="text-gray-700"><span class="font-semibold">Growth Trend:</span> <span
                                            id="growthTrend">+18.5%</span></p>
                                    <p class="text-gray-700"><span class="font-semibold">Best Seller:</span> <span
                                            id="bestSeller">Casual Round Neck</span></p>
                                    <p class="text-gray-700"><span class="font-semibold">Forecast Method:</span> <span
                                            id="method">Exponential Smoothing</span></p>
                                </div>
                            </div>
                        </div>
                    </div>

                    <div class="bg-white rounded-xl shadow-sm p-6">
                        <div class="flex flex-col sm:flex-row sm:items-center sm:justify-between mb-6">
                            <div class="flex items-center space-x-3 mb-4 sm:mb-0">
                                <div class="w-10 h-10 bg-emerald-100 rounded-lg flex items-center justify-center">
                                    <i data-lucide="bar-chart-3" class="w-5 h-5 text-emerald-600"></i>
                                </div>
                                <div class="flex items-center space-x-2">
                                    <h2 class="text-xl font-semibold text-gray-900 tracking-tight">AI Recommendations
                                    </h2>
                                    <span class="live-dot w-2 h-2 bg-emerald-500 rounded-full"></span>
                                </div>
                            </div>
                            <div id="recommendationsButtonContainer">
                                <button id="generateRecommendationsBtn" onclick="generateRecommendations()"
                                    class="inline-flex items-center px-4 py-2 bg-emerald-500 hover:bg-emerald-600 text-white text-sm font-medium rounded-lg transition-colors">
                                    <i data-lucide="bar-chart-3" class="w-4 h-4 mr-2"></i>
                                    Generate Predictions
                                </button>
                            </div>
                        </div>
                        <div class="mb-6">
                            <p class="text-sm text-gray-600">
                                ⚠️ <span class="font-semibold text-gray-900">4 Pending Insights</span> detected in
                                Inventory and Pricing.
                            </p>
                        </div>

                        <!-- Capability Tags -->
                        <div class="flex flex-wrap items-center gap-3 mb-6" id="capabilityTags">
                            <div
                                class="inline-flex items-center px-3 py-1.5 bg-blue-50 border border-blue-100 rounded-lg opacity-75">
                                <i data-lucide="package-search" class="w-4 h-4 text-blue-600 mr-2"></i>
                                <span class="text-xs font-medium text-blue-700">Inventory Analysis</span>
                            </div>
                            <div
                                class="inline-flex items-center px-3 py-1.5 bg-purple-50 border border-purple-100 rounded-lg opacity-75">
                                <i data-lucide="dollar-sign" class="w-4 h-4 text-purple-600 mr-2"></i>
                                <span class="text-xs font-medium text-purple-700">Price Optimization</span>
                            </div>
                            <div
                                class="inline-flex items-center px-3 py-1.5 bg-cyan-50 border border-cyan-100 rounded-lg opacity-75">
                                <i data-lucide="trending-up" class="w-4 h-4 text-cyan-600 mr-2"></i>
                                <span class="text-xs font-medium text-cyan-700">Trend Forecasting</span>
                            </div>
                        </div>

                        <!-- Empty State -->
                        <div id="recommendationsEmptyState"
                            class="flex flex-col items-center justify-center py-16 text-center">
                            <i data-lucide="bar-chart-3" class="w-16 h-16 text-gray-400 mb-4"></i>
                            <p class="text-gray-500 text-lg font-medium">AI Recommendation</p>
                            <p class="text-gray-400 text-sm mt-2">Click Generate Predictions to see recommendations</p>
                        </div>

                        <!-- Loading State -->
                        <div id="recommendationsLoadingState" class="hidden">
                            <div class="flex flex-col items-center justify-center py-16">
                                <div class="w-full max-w-md">
                                    <div class="mb-4">
                                        <div class="h-2 bg-gray-200 rounded-full overflow-hidden">
                                            <div id="loadingProgressBar"
                                                class="h-full bg-gradient-to-r from-purple-500 to-blue-500 rounded-full transition-all duration-500"
                                                style="width: 0%"></div>
                                        </div>
                                    </div>
                                    <p id="loadingStepText" class="text-center text-sm font-medium text-gray-600">
                                        Analyzing Sales Data...</p>
                                </div>
                            </div>
                        </div>

                        <!-- Results Grid -->
                        <div id="recommendationsResults" class="hidden grid grid-cols-1 md:grid-cols-2 gap-6">
                            <!-- Card 1: Price Optimization -->
                            <div class="ai-insight-card relative bg-white rounded-xl p-6">
                                <div class="flex items-start justify-between mb-4">
                                    <div class="flex items-center space-x-3">
                                        <div
                                            class="w-10 h-10 bg-purple-100 rounded-lg flex items-center justify-center">
                                            <i data-lucide="dollar-sign" class="w-5 h-5 text-purple-600"></i>
                                        </div>
                                        <h3 class="text-lg font-semibold text-gray-900">Price Optimization</h3>
                                    </div>
                                    <span
                                        class="inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-medium bg-emerald-100 text-emerald-700 border border-emerald-200">
                                        High Confidence
                                    </span>
                                </div>
                                <p class="text-sm text-gray-600 mb-4">
                                    Demand for <span class="font-semibold text-gray-900">Heavy Tee (Black)</span> is up
                                    25% in Lahore. Suggest increasing price by <span
                                        class="font-semibold text-emerald-700">Rs 200</span>.
                                </p>
                                <button onclick="applyPriceChange()"
                                    class="w-full px-4 py-2 bg-emerald-500 hover:bg-emerald-600 text-white text-sm font-medium rounded-lg transition-colors">
                                    Apply Price Change
                                </button>
                            </div>

                            <!-- Card 2: Stock Alert -->
                            <div class="ai-insight-card relative bg-white rounded-xl p-6">
                                <div class="flex items-start justify-between mb-4">
                                    <div class="flex items-center space-x-3">
                                        <div class="w-10 h-10 bg-red-100 rounded-lg flex items-center justify-center">
                                            <i data-lucide="alert-triangle" class="w-5 h-5 text-red-600"></i>
                                        </div>
                                        <h3 class="text-lg font-semibold text-gray-900">Stock Alert</h3>
                                    </div>
                                    <span
                                        class="inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-medium bg-red-100 text-red-700 border border-red-200">
                                        Urgent
                                    </span>
                                </div>
                                <p class="text-sm text-gray-600 mb-4">
                                    Inventory for <span class="font-semibold text-gray-900">Premium Blank Tee (Olive,
                                        M)</span> will run out in <span class="font-semibold text-red-700">3 days</span>
                                    based on current order rate.
                                </p>
                                <button onclick="orderStock()"
                                    class="w-full px-4 py-2 bg-red-500 hover:bg-red-600 text-white text-sm font-medium rounded-lg transition-colors">
                                    Order Stock
                                </button>
                            </div>
                        </div>
                    </div>
                </div>


                <div id="roles-tab" class="tab-content">
                    <div class="bg-white rounded-xl shadow-sm p-6 border border-gray-100">
                        <div class="flex items-center justify-between mb-6">
                            <div class="flex items-center space-x-4">
                                <div
                                    class="w-12 h-12 bg-purple-100 rounded-xl flex items-center justify-center shrink-0">
                                    <i data-lucide="users" class="w-6 h-6 text-purple-600"></i>
                                </div>
                                <div>
                                    <h2 class="text-lg font-bold text-gray-900 tracking-tight">Team Members</h2>
                                    <p class="text-sm text-gray-500 font-medium">Manage team access and permissions</p>
                                </div>
                            </div>
                            <button onclick="inviteMember()"
                                class="inline-flex items-center px-4 py-2 bg-emerald-500 hover:bg-emerald-600 text-white text-sm font-semibold rounded-lg shadow-sm transition-all hover:shadow hover:-translate-y-0.5">
                                <i data-lucide="plus" class="w-4 h-4 mr-2"></i>
                                Invite Member
                            </button>
                        </div>

                        <!-- Team Members Table -->
                        <div class="overflow-x-auto">
                            <table class="min-w-full divide-y divide-gray-200">
                                <thead class="bg-gray-50 sticky top-0 z-10">
                                    <tr>
                                        <th
                                            class="px-6 py-3 text-left text-xs font-semibold tracking-wider text-gray-500 uppercase">
                                            User</th>
                                        <th
                                            class="px-6 py-3 text-left text-xs font-semibold tracking-wider text-gray-500 uppercase">
                                            Role</th>
                                        <th
                                            class="px-6 py-3 text-left text-xs font-semibold tracking-wider text-gray-500 uppercase">
                                            Status</th>
                                        <th
                                            class="px-6 py-3 text-left text-xs font-semibold tracking-wider text-gray-500 uppercase">
                                            Last Active</th>
                                        <th
                                            class="px-6 py-3 text-left text-xs font-semibold tracking-wider text-gray-500 uppercase">
                                            Actions</th>
                                    </tr>
                                </thead>
                                <tbody class="bg-white divide-y divide-gray-200" id="usersTableBody">
                                    <!-- Users will be populated by JavaScript -->
                                </tbody>
                            </table>
                        </div>
                    </div>
                </div>

                <div id="logs-tab" class="tab-content">
                    <div class="bg-white rounded-xl shadow-sm p-6">
                        <div class="flex items-center space-x-3 mb-6">
                            <div class="w-10 h-10 bg-cyan-100 rounded-lg flex items-center justify-center">
                                <i data-lucide="clock" class="w-5 h-5 text-cyan-600"></i>
                            </div>
                            <div>
                                <h2 class="text-xl font-semibold text-gray-900 tracking-tight">Activity Timeline</h2>
                                <p class="text-sm text-gray-500">Audit trail of system activities and changes</p>
                            </div>
                        </div>

                        <!-- Timeline Container -->
                        <div class="relative">
                            <!-- Vertical Timeline Line -->
                            <div class="absolute left-6 top-0 bottom-0 w-0.5 bg-gray-200"></div>

                            <!-- Timeline Items -->
                            <div class="space-y-6" id="activityTimeline">
                                <!-- Timeline items will be populated by JavaScript -->
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </main>

    <!-- Slide-Over Drawer for Add Product -->
    <div id="productDrawer"
        class="fixed inset-y-0 right-0 z-50 w-full max-w-2xl bg-white shadow-2xl transform translate-x-full transition-transform duration-300 ease-in-out">
        <div class="flex flex-col h-full">
            <!-- Drawer Header -->
            <div class="flex items-center justify-between p-6 border-b border-gray-200">
                <h2 class="text-2xl font-semibold text-gray-900">Add New Product</h2>
                <button onclick="closeProductDrawer()" class="p-2 rounded-lg hover:bg-gray-100 transition-colors"
                    title="Close drawer" aria-label="Close">
                    <i data-lucide="x" class="w-6 h-6 text-gray-400"></i>
                </button>
            </div>

            <!-- Drawer Content -->
            <div class="flex-1 overflow-y-auto p-6">
                <form id="productForm" class="space-y-6">
                    <div>
                        <label class="block text-base font-medium text-gray-900 mb-2">Product Name</label>
                        <input type="text" id="productName"
                            class="w-full px-4 py-3 border-2 border-gray-200 rounded-xl focus:outline-none focus:ring-2 focus:ring-cyan-500 focus:border-transparent"
                            placeholder="Enter product name">
                    </div>

                    <div>
                        <label class="block text-base font-medium text-gray-900 mb-2">Description</label>
                        <textarea id="productDescription" rows="4"
                            class="w-full px-4 py-3 border-2 border-gray-200 rounded-xl focus:outline-none focus:ring-2 focus:ring-cyan-500 focus:border-transparent resize-none"
                            placeholder="Enter product description"></textarea>
                    </div>

                    <div>
                        <label class="block text-base font-medium text-gray-900 mb-2">Base Price (Rs)</label>
                        <input type="number" id="productPrice" step="0.01"
                            class="w-full px-4 py-3 border-2 border-gray-200 rounded-xl focus:outline-none focus:ring-2 focus:ring-cyan-500 focus:border-transparent"
                            placeholder="0.00">
                    </div>

                    <div>
                        <label class="block text-base font-medium text-gray-900 mb-2">Image URL</label>
                        <input type="url" id="productImage"
                            class="w-full px-4 py-3 border-2 border-gray-200 rounded-xl focus:outline-none focus:ring-2 focus:ring-cyan-500 focus:border-transparent"
                            placeholder="https://example.com/image.jpg">
                    </div>

                    <div>
                        <label class="block text-base font-medium text-gray-900 mb-2">Category</label>
                        <input type="text" id="productCategory"
                            class="w-full px-4 py-3 border-2 border-gray-200 rounded-xl focus:outline-none focus:ring-2 focus:ring-cyan-500 focus:border-transparent"
                            placeholder="Enter category">
                    </div>

                    <div class="flex items-center">
                        <input type="checkbox" id="featureProduct"
                            class="w-4 h-4 text-cyan-400 border-gray-300 rounded focus:ring-cyan-500 cursor-pointer">
                        <label for="featureProduct" class="ml-2 text-base font-medium text-gray-900 cursor-pointer">
                            Feature Product
                        </label>
                    </div>

                    <div class="flex space-x-4">
                        <button type="submit"
                            class="flex-1 py-3 bg-cyan-400 hover:bg-cyan-500 text-white font-medium rounded-full transition-colors text-base">
                            Create Product
                        </button>
                        <button type="button" onclick="closeProductDrawer()"
                            class="flex-1 py-3 bg-gray-100 hover:bg-gray-200 text-gray-700 font-medium rounded-full transition-colors text-base">
                            Cancel
                        </button>
                    </div>
                </form>
            </div>
        </div>
    </div>

    <!-- Toast Notifications Container -->
    <div id="toastContainer" class="fixed top-4 right-4 z-50 space-y-2"></div>

    <script>
        // Use shared data from ShirtifyData

        // Team members data - integrated with ShirtifyData
        var getTeamMembers = function () {
            // Return users from ShirtifyData or default team members
            return ShirtifyData.users || [
                {
                    id: "usr_001",
                    name: "Balaj Mir",
                    email: "balaj.mir@shirtify.pk",
                    role: "Admin",
                    status: "Active",
                    lastActive: "2 hours ago"
                },
                {
                    id: "usr_002",
                    name: "Abubakar Shahzad",
                    email: "abubakar.shahzad@shirtify.pk",
                    role: "Manager",
                    status: "Active",
                    lastActive: "1 hour ago"
                },
                {
                    id: "usr_003",
                    name: "Adeen Siddique",
                    email: "adeen.siddique@shirtify.pk",
                    role: "Editor",
                    status: "Active",
                    lastActive: "30 minutes ago"
                }
            ];
        };

        // Activity logs data - integrated with ShirtifyData
        var getActivityLogs = function () {
            // Return recent activity from ShirtifyData or default logs
            return ShirtifyData.recentActivity && ShirtifyData.recentActivity.length > 0 ?
                ShirtifyData.recentActivity.map(activity => ({
                    id: activity.id,
                    type: activity.type,
                    title: activity.title,
                    description: activity.description,
                    user: activity.type === 'auth' ? activity.description.split(' ')[1] + ' ' + activity.description.split(' ')[2] : 'System',
                    timestamp: activity.time,
                    icon: activity.icon,
                    color: activity.iconColor
                })) :
                [
                    {
                        id: "log_001",
                        type: "login",
                        title: "User Login",
                        description: "Ahmed Hassan logged into the admin dashboard",
                        user: "Ahmed Hassan",
                        timestamp: "2 minutes ago",
                        icon: "log-in",
                        color: "blue"
                    },
                    {
                        id: "log_002",
                        type: "create",
                        title: "Product Created",
                        description: "New product 'Urban Streetwear Graphic' was added to catalog",
                        user: "Zainab Khan",
                        timestamp: "15 minutes ago",
                        icon: "plus",
                        color: "green"
                    },
                    {
                        id: "log_003",
                        type: "settings",
                        title: "System Updated",
                        description: "Admin changed the price of Heavy Tee (Black) from Rs 2,500 to Rs 2,700",
                        user: "System",
                        timestamp: "1 hour ago",
                        icon: "settings",
                        color: "gray"
                    },
                    {
                        id: "log_004",
                        type: "delete",
                        title: "Product Removed",
                        description: "Discontinued product 'Old Logo Tee' was removed from catalog",
                        user: "Ahmed Hassan",
                        timestamp: "2 hours ago",
                        icon: "trash-2",
                        color: "red"
                    },
                    {
                        id: "log_005",
                        type: "login",
                        title: "User Login",
                        description: "Fatima Ahmed logged into the admin dashboard",
                        user: "Fatima Ahmed",
                        timestamp: "3 hours ago",
                        icon: "log-in",
                        color: "blue"
                    },
                    {
                        id: "log_006",
                        type: "create",
                        title: "Order Processed",
                        description: "Order #ORD-7783 marked as shipped and tracking number added",
                        user: "Zainab Khan",
                        timestamp: "4 hours ago",
                        icon: "package",
                        color: "green"
                    }
                ];
        };

        // Get all products from shared data
        function getAllProducts() {
            return ShirtifyData.products;
        }

        // Filter products based on filters
        function filterProducts() {
            var colorFilter = document.getElementById('filterColor').value;
            var sizeFilter = document.getElementById('filterSize').value;
            var priceMin = document.getElementById('priceMin').value ? parseFloat(document.getElementById('priceMin').value) : null;
            var priceMax = document.getElementById('priceMax').value ? parseFloat(document.getElementById('priceMax').value) : null;

            var filters = {};
            if (colorFilter) filters.color = colorFilter;
            if (sizeFilter) filters.size = sizeFilter;
            if (priceMin !== null) filters.minPrice = priceMin;
            if (priceMax !== null) filters.maxPrice = priceMax;

            var filteredProducts = ShirtifyData.getFilteredProducts(filters);
            renderProductsTable(filteredProducts);
        }

        // Render products table
        function renderProductsTable(productsToRender) {
            var products = productsToRender || getAllProducts();
            var tbody = document.getElementById('productsTableBody');
            var emptyState = document.getElementById('productsEmptyState');


            if (products.length === 0) {
                tbody.innerHTML = '';
                emptyState.classList.remove('hidden');
                return;
            }

            emptyState.classList.add('hidden');

            var html = '';
            for (var i = 0; i < products.length; i++) {
                var product = products[i];
                var colorInfo = ShirtifyData.colorMap[product.baseColor] || ShirtifyData.colorMap['Black'];
                var sizesText = product.sizes.join(', ');

                html += '<tr class="hover:bg-gray-50">';

                // Thumbnail
                html += '<td class="px-6 py-4 whitespace-nowrap">';
                if (product.image) {
                    html += '<div class="w-16 h-16 bg-gray-200 rounded-lg overflow-hidden flex-shrink-0">';
                    html += '<img src="' + product.image + '" alt="' + (product.designName || 'Product') + '" class="w-full h-full object-cover rounded-lg" onerror="this.src=\'https://via.placeholder.com/100?text=T-Shirt\'">';
                    html += '</div>';
                } else {
                    html += '<div class="w-16 h-16 bg-gray-200 rounded-lg flex items-center justify-center">';
                    html += '<i data-lucide="shirt" class="w-8 h-8 text-gray-400"></i>';
                    html += '</div>';
                }
                html += '</td>';

                // Design Info
                html += '<td class="px-6 py-4 whitespace-nowrap">';
                html += '<div class="text-sm font-semibold text-gray-900">' + (product.designName || 'Unnamed Design') + '</div>';
                html += '<div class="text-xs text-gray-500 mt-1">Print Type: ' + (product.printType || 'DTF') + '</div>';
                html += '</td>';

                // Base Color
                html += '<td class="px-6 py-4 whitespace-nowrap">';
                html += '<div class="flex items-center space-x-2">';
                var borderStyle = colorInfo.border ? 'border border-gray-300' : '';
                html += '<div class="w-5 h-5 rounded-full ' + borderStyle + '" style="background-color: ' + colorInfo.bg + '"></div>';
                html += '<span class="text-sm text-gray-700">' + colorInfo.name + '</span>';
                html += '</div>';
                html += '</td>';

                // Sizes/Variants
                html += '<td class="px-6 py-4 whitespace-nowrap">';
                html += '<span class="text-sm text-gray-700">' + sizesText + '</span>';
                html += '</td>';

                // Price
                html += '<td class="px-6 py-4 whitespace-nowrap">';
                html += '<div class="text-sm font-semibold text-emerald-700">Rs ' + product.price.toLocaleString('en-PK') + '</div>';
                html += '</td>';

                // Stock Status
                html += '<td class="px-6 py-4 whitespace-nowrap">';
                html += '<span class="inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-medium border ' + product.stockBadgeClass + '">' + product.stockBadge + '</span>';
                html += '</td>';

                // Actions
                html += '<td class="px-6 py-4 whitespace-nowrap text-sm font-medium">';
                html += '<div class="flex items-center space-x-2">';
                html += '<button onclick="editProduct(' + i + ')" class="w-8 h-8 bg-white border border-gray-200 rounded-lg flex items-center justify-center hover:bg-gray-50 transition-colors" aria-label="Edit">';
                html += '<i data-lucide="pencil" class="w-4 h-4 text-gray-600"></i>';
                html += '</button>';
                html += '<button onclick="deleteProduct(' + i + ')" class="w-8 h-8 bg-red-50 border border-red-100 rounded-lg flex items-center justify-center hover:bg-red-100 transition-colors" aria-label="Delete">';
                html += '<i data-lucide="trash-2" class="w-4 h-4 text-red-500"></i>';
                html += '</button>';
                html += '</div>';
                html += '</td>';

                html += '</tr>';
            }

            tbody.innerHTML = html;
            lucide.createIcons();
        }

        // Delete product
        function deleteProduct(index) {
            if (confirm('Are you sure you want to delete this product?')) {
                var allProducts = getAllProducts();
                var product = allProducts[index];
                if (product) {
                    ShirtifyData.deleteProduct(product.id);
                    filterProducts();
                    updateKPIs();
                }
            }
        }

        // Edit product (open drawer for editing)
        function editProduct(index) {
            var allProducts = getAllProducts();
            if (allProducts[index]) {
                // Populate the form with existing product data
                var product = allProducts[index];
                document.getElementById('productName').value = product.name || product.designName || '';
                document.getElementById('productDescription').value = product.description || '';
                document.getElementById('productPrice').value = product.price || '';
                document.getElementById('productImage').value = product.image || '';
                document.getElementById('productCategory').value = product.category || '';
                document.getElementById('featureProduct').checked = product.featured || false;

                // Store the product ID for editing
                document.getElementById('productForm').setAttribute('data-edit-id', product.id);

                // Change button text
                var submitBtn = document.querySelector('#productForm button[type="submit"]');
                if (submitBtn) submitBtn.textContent = 'Update Product';

                openProductDrawer();
            }
        }

        // Update KPI cards
        function updateKPIs() {
            var kpis = ShirtifyData.getKPIs();
            document.getElementById('totalProducts').textContent = kpis.totalProducts || 0;
            document.getElementById('totalSales').textContent = 'Rs ' + (kpis.recentSales || 0).toLocaleString('en-PK');
            document.getElementById('totalOrders').textContent = kpis.recentOrders || 0;
            document.getElementById('totalVisitors').textContent = kpis.totalCustomers || 0;

            // Update growth percentages if available
            if (kpis.salesGrowth !== undefined) {
                updateGrowthIndicator('totalSales', kpis.salesGrowth);
            }
            if (kpis.ordersGrowth !== undefined) {
                updateGrowthIndicator('totalOrders', kpis.ordersGrowth);
            }
            if (kpis.customersGrowth !== undefined) {
                updateGrowthIndicator('totalVisitors', kpis.customersGrowth);
            }
        }

        function updateGrowthIndicator(elementId, growth) {
            var element = document.querySelector('#' + elementId).closest('.kpi-card');
            var growthSpan = element.querySelector('.text-xs.font-medium');
            if (growthSpan && growth !== undefined) {
                var growthText = (growth >= 0 ? '+' : '') + growth.toFixed(1) + '%';
                growthSpan.textContent = growthText;
                growthSpan.className = 'text-xs font-medium ' +
                    (growth >= 0 ? 'text-emerald-600 bg-emerald-50' : 'text-red-600 bg-red-50') +
                    ' px-2 py-1 rounded-full';
            }
        }

        function renderRecentActivity() {
            var activityContainer = document.getElementById('recentActivity');
            var html = '';

            for (var i = 0; i < ShirtifyData.recentActivity.length; i++) {
                var activity = ShirtifyData.recentActivity[i];
                var iconBgClass = 'bg-' + activity.iconColor + '-100';
                var iconTextClass = 'text-' + activity.iconColor + '-600';

                html += '<div class="activity-item flex items-start space-x-4 p-4 bg-gray-50 rounded-lg border border-gray-100">';
                html += '<div class="w-8 h-8 ' + iconBgClass + ' rounded-full flex items-center justify-center flex-shrink-0">';
                html += '<i data-lucide="' + activity.icon + '" class="w-4 h-4 ' + iconTextClass + '"></i>';
                html += '</div>';
                html += '<div class="flex-1 min-w-0">';
                html += '<p class="text-sm font-medium text-gray-900">' + activity.title + '</p>';
                html += '<p class="text-xs text-gray-500 mt-1">' + activity.description + '</p>';
                html += '<p class="text-xs text-gray-400 mt-1">' + activity.time + '</p>';
                html += '</div>';
                html += '</div>';
            }

            activityContainer.innerHTML = html;
            lucide.createIcons();
        }

        // Pre-computed role badge classes for performance
        var roleBadgeClasses = {
            'Admin': 'bg-purple-100 text-purple-700 border-purple-200',
            'Manager': 'bg-blue-100 text-blue-700 border-blue-200',
            'Editor': 'bg-emerald-100 text-emerald-700 border-emerald-200',
            'Viewer': 'bg-gray-100 text-gray-700 border-gray-200'
        };

        // Sparkline functionality for KPI cards
        function renderSparklines() {
            if (typeof Chart === 'undefined') return;

            var analytics = ShirtifyData.analyticsData;
            if (!analytics || !analytics.units) return;

            // Sales sparkline
            renderSparkline('salesSparkline', analytics.revenue || analytics.units, '#10b981');

            // Orders sparkline
            renderSparkline('ordersSparkline', analytics.units, '#3b82f6');

            // Products sparkline (simulate growth)
            var productsData = analytics.units.map(val => Math.floor(val * 0.3));
            renderSparkline('productsSparkline', productsData, '#f59e0b');

            // Customers sparkline (simulate customer growth)
            var customersData = analytics.units.map((val, idx) => Math.floor(val * 0.15 * (idx + 1)));
            renderSparkline('customersSparkline', customersData, '#8b5cf6');
        }

        function renderSparkline(canvasId, data, color) {
            var canvas = document.getElementById(canvasId);
            if (!canvas) return;

            var ctx = canvas.getContext('2d');
            var sparkline = new Chart(ctx, {
                type: 'line',
                data: {
                    labels: data.map((_, i) => i),
                    datasets: [{
                        data: data,
                        borderColor: color,
                        backgroundColor: 'transparent',
                        borderWidth: 2,
                        pointRadius: 0,
                        tension: 0.4,
                        fill: false
                    }]
                },
                options: {
                    responsive: true,
                    maintainAspectRatio: false,
                    plugins: {
                        legend: { display: false },
                        tooltip: { enabled: false }
                    },
                    scales: {
                        x: { display: false },
                        y: { display: false }
                    },
                    elements: {
                        point: { hoverRadius: 0 }
                    }
                }
            });
        }

        // Avatar generation function
        function generateAvatar(initials, colorClass = 'bg-gray-200') {
            return `<div class="w-10 h-10 ${colorClass} rounded-full flex items-center justify-center">
                <span class="text-sm font-semibold text-white">${initials}</span>
            </div>`;
        }

        // Get user avatar color based on name
        function getAvatarColor(name) {
            var colors = ['bg-blue-500', 'bg-emerald-500', 'bg-purple-500', 'bg-amber-500', 'bg-red-500', 'bg-indigo-500'];
            var index = name.length % colors.length;
            return colors[index];
        }

        // Render users table - optimized for performance
        function renderUsersTable() {
            var tbody = document.getElementById('usersTableBody');
            if (!tbody) return;

            var users = ShirtifyData.users || []; // Use real users from API

            var html = '';

            for (var i = 0; i < users.length; i++) {
                var user = users[i];
                var roleClass = roleBadgeClasses[user.role] || 'bg-gray-100 text-gray-700 border-gray-200';
                var statusClass = user.status === 'Active' ? 'bg-emerald-500' : 'bg-gray-400';
                var initial = user.name.charAt(0).toUpperCase();

                html += '<tr class="hover:bg-gray-50 transition-colors">' +
                    '<td class="px-6 py-4 whitespace-nowrap">' +
                    '<div class="flex items-center space-x-3">' +
                    generateAvatar(initial, getAvatarColor(user.name)) +
                    '<div>' +
                    '<div class="text-sm font-semibold text-gray-900">' + user.name + '</div>' +
                    '<div class="text-sm text-gray-500">' + user.email + '</div>' +
                    '</div>' +
                    '</div>' +
                    '</td>' +
                    '<td class="px-6 py-4 whitespace-nowrap">' +
                    '<span class="inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-medium border ' + roleClass + '">' + user.role + '</span>' +
                    '</td>' +
                    '<td class="px-6 py-4 whitespace-nowrap">' +
                    '<div class="flex items-center space-x-2">' +
                    '<div class="w-2 h-2 rounded-full ' + statusClass + '"></div>' +
                    '<span class="text-sm text-gray-700">' + user.status + '</span>' +
                    '</div>' +
                    '</td>' +
                    '<td class="px-6 py-4 whitespace-nowrap">' +
                    '<span class="text-sm text-gray-700">' + user.lastActive + '</span>' +
                    '</td>' +
                    '<td class="px-6 py-4 whitespace-nowrap text-sm font-medium">' +
                    '<div class="flex items-center space-x-2">' +
                    '<button onclick="editUser(\'' + user.id + '\')" class="w-8 h-8 bg-white border border-gray-200 rounded-lg flex items-center justify-center hover:bg-gray-50 transition-colors" aria-label="Edit">' +
                    '<i data-lucide="pencil" class="w-4 h-4 text-gray-600"></i>' +
                    '</button>' +
                    '<button onclick="deleteUser(\'' + user.id + '\')" class="w-8 h-8 bg-red-50 border border-red-100 rounded-lg flex items-center justify-center hover:bg-red-100 transition-colors" aria-label="Delete">' +
                    '<i data-lucide="trash-2" class="w-4 h-4 text-red-500"></i>' +
                    '</button>' +
                    '</div>' +
                    '</td>' +
                    '</tr>';
            }

            tbody.innerHTML = html;
            lucide.createIcons();
        }

        // Pre-computed icon classes for performance
        var iconBgClasses = {
            'blue': 'bg-blue-100',
            'green': 'bg-emerald-100',
            'red': 'bg-red-100',
            'gray': 'bg-gray-100'
        };

        // Render activity timeline - optimized for performance
        function renderActivityTimeline() {
            var timelineContainer = document.getElementById('activityTimeline');
            if (!timelineContainer) return;

            var logs = getActivityLogs();

            var html = '';

            for (var i = 0; i < logs.length; i++) {
                var log = logs[i];
                var bgClass = iconBgClasses[log.color] || 'bg-gray-100';
                var textClass = 'text-' + log.color + '-600';

                html += '<div class="relative flex items-start space-x-4 pb-6">' +
                    '<div class="relative flex-shrink-0">' +
                    '<div class="w-12 h-12 ' + bgClass + ' rounded-full flex items-center justify-center border-4 border-white shadow-sm">' +
                    '<i data-lucide="' + log.icon + '" class="w-5 h-5 ' + textClass + '"></i>' +
                    '</div>' +
                    '</div>' +
                    '<div class="flex-1 min-w-0 pb-6">' +
                    '<div class="flex items-start justify-between">' +
                    '<div class="flex-1">' +
                    '<h4 class="text-sm font-semibold text-gray-900">' + log.title + '</h4>' +
                    '<p class="text-sm text-gray-600 mt-1">' + log.description + '</p>' +
                    '</div>' +
                    '<div class="flex-shrink-0 text-right ml-4">' +
                    '<p class="text-xs text-gray-500">' + log.timestamp + '</p>' +
                    '<p class="text-xs text-gray-400 mt-1">by ' + log.user + '</p>' +
                    '</div>' +
                    '</div>' +
                    '</div>' +
                    '</div>';
            }

            timelineContainer.innerHTML = html;
            lucide.createIcons();
        }

        function renderOrders() {
            console.log('Rendering orders, found', ShirtifyData.orders.length, 'orders');

            // Update pipeline summary cards
            updateOrderPipelineSummary();

            // Render all orders (filtering will be applied separately)
            renderFilteredOrders(ShirtifyData.orders);
        }

        function updateOrderPipelineSummary() {
            var orders = ShirtifyData.orders;
            var statusCounts = {
                'Pending': 0,
                'Processing': 0,
                'Shipped': 0,
                'Delivered': 0
            };

            // Count orders by status
            orders.forEach(function (order) {
                if (statusCounts.hasOwnProperty(order.status)) {
                    statusCounts[order.status]++;
                }
            });

            // Update the pipeline cards
            var statusMappings = {
                'Pending': { element: 'pendingOrders', color: 'amber' },
                'Processing': { element: 'processingOrders', color: 'blue' },
                'Shipped': { element: 'shippedOrders', color: 'purple' },
                'Delivered': { element: 'deliveredOrders', color: 'emerald' }
            };

            Object.keys(statusMappings).forEach(function (status) {
                var mapping = statusMappings[status];
                var element = document.getElementById(mapping.element);
                if (element) {
                    element.textContent = statusCounts[status];
                }
            });
        }

        function updateOrderStatus(orderId, newStatus) {
            ShirtifyData.updateOrderStatus(orderId, newStatus);
            renderOrders();
            lucide.createIcons();
        }

        function filterOrders() {
            var searchTerm = document.getElementById('orderSearch').value.toLowerCase();
            var statusFilter = document.getElementById('statusFilter').value;
            var sortBy = document.getElementById('sortOrders').value;

            var filteredOrders = ShirtifyData.orders.filter(function (order) {
                var matchesSearch = !searchTerm ||
                    order.id.toLowerCase().includes(searchTerm) ||
                    order.customer.toLowerCase().includes(searchTerm) ||
                    order.location.toLowerCase().includes(searchTerm) ||
                    order.items.toLowerCase().includes(searchTerm);

                var matchesStatus = !statusFilter || order.status === statusFilter;

                return matchesSearch && matchesStatus;
            });

            // Sort orders
            filteredOrders.sort(function (a, b) {
                switch (sortBy) {
                    case 'date-asc':
                        return new Date(a.orderDate) - new Date(b.orderDate);
                    case 'date-desc':
                        return new Date(b.orderDate) - new Date(a.orderDate);
                    case 'amount-asc':
                        return a.amount - b.amount;
                    case 'amount-desc':
                        return b.amount - a.amount;
                    default:
                        return new Date(b.orderDate) - new Date(a.orderDate);
                }
            });

            renderFilteredOrders(filteredOrders);
        }

        // Analytics charts rendering function
        function renderAnalyticsCharts() {
            if (typeof Chart === 'undefined') return;

            // Initialize forecast chart
            initForecastChart();

            // Initialize category chart
            initCategoryChart();

            // Initialize size chart
            initSizeChart();

            // Initialize current date
            var dateOptions = { weekday: 'long', year: 'numeric', month: 'long', day: 'numeric' };
            var today = new Date().toLocaleDateString('en-US', dateOptions);
            var dateEl = document.getElementById('currentDate');
            if (dateEl) dateEl.textContent = today;


            // Populate analytics data
            populateTopProducts();
            populateProductTable();
            updateAnalyticsStatistics();

            // Initialize Sales Analytics (Feature 1)
            initSalesAnalytics();
        }

        let salesTrendsChart, salesSizeChart, topDesignsChart;

        async function initSalesAnalytics() {
            // Set default dates (last 30 days)
            const end = new Date();
            const start = new Date();
            start.setDate(start.getDate() - 30);
            
            document.getElementById('salesStartDate').value = start.toISOString().split('T')[0];
            document.getElementById('salesEndDate').value = end.toISOString().split('T')[0];
            
            updateSalesAnalytics();
        }

        async function updateSalesAnalytics() {
            const startDate = document.getElementById('salesStartDate').value;
            const endDate = document.getElementById('salesEndDate').value;
            const interval = document.getElementById('salesInterval').value;

            try {
                const result = await ShirtifyAPI.getSalesAnalytics(startDate, endDate, interval);
                if (result.success && result.data) {
                    renderSalesCharts(result.data);
                } else {
                    showToast('Failed to fetch analytics data', 'error');
                }
            } catch (error) {
                console.error('Analytics Fetch Error:', error);
            }
        }

        function renderSalesCharts(data) {
            const { trends, sizeDistribution, topDesigns } = data;

            // 1. Order Trends Chart
            const trendCtx = document.getElementById('salesTrendsChart').getContext('2d');
            if (salesTrendsChart) salesTrendsChart.destroy();
            salesTrendsChart = new Chart(trendCtx, {
                type: 'line',
                data: {
                    labels: trends.map(t => t._id),
                    datasets: [
                        {
                            label: 'Order Count',
                            data: trends.map(t => t.orderCount),
                            borderColor: '#10b981',
                            backgroundColor: 'rgba(16, 185, 129, 0.1)',
                            fill: true,
                            tension: 0.4
                        },
                        {
                            label: 'Revenue (Rs)',
                            data: trends.map(t => t.totalRevenue),
                            borderColor: '#3b82f6',
                            borderDash: [5, 5],
                            pointRadius: 0,
                            yAxisID: 'y1',
                            tension: 0.4
                        }
                    ]
                },
                options: {
                    responsive: true,
                    maintainAspectRatio: false,
                    scales: {
                        y: { beginAtZero: true, grid: { display: false } },
                        y1: { position: 'right', beginAtZero: true, grid: { display: false } }
                    },
                    plugins: { legend: { position: 'top', labels: { usePointStyle: true } } }
                }
            });

            // 2. Size Distribution Chart
            const sizeCtx = document.getElementById('salesSizeChart').getContext('2d');
            if (salesSizeChart) salesSizeChart.destroy();
            salesSizeChart = new Chart(sizeCtx, {
                type: 'doughnut',
                data: {
                    labels: sizeDistribution.map(s => s._id),
                    datasets: [{
                        data: sizeDistribution.map(s => s.count),
                        backgroundColor: ['#60a5fa', '#34d399', '#fbbf24', '#a78bfa', '#f472b6']
                    }]
                },
                options: {
                    responsive: true,
                    maintainAspectRatio: false,
                    plugins: { legend: { display: false } }
                }
            });

            // 3. Top Designs Chart
            const designsCtx = document.getElementById('topDesignsChart').getContext('2d');
            if (topDesignsChart) topDesignsChart.destroy();
            topDesignsChart = new Chart(designsCtx, {
                type: 'bar',
                data: {
                    labels: topDesigns.map(d => d._id),
                    datasets: [{
                        label: 'Units Sold',
                        data: topDesigns.map(d => d.quantity),
                        backgroundColor: '#6366f1',
                        borderRadius: 8
                    }]
                },
                options: {
                    responsive: true,
                    maintainAspectRatio: false,
                    scales: { x: { grid: { display: false } }, y: { beginAtZero: true } },
                    indexAxis: 'y'
                }
            });
        }

        function initForecastChart() {
            var demoDataset = ShirtifyData.analyticsData || { dates: [], units: [] };

            var ctx = document.getElementById('forecastChart').getContext('2d');
            var forecastData = createForecastData(demoDataset.units);

            var futureMonths = ['Oct 2024', 'Nov 2024', 'Dec 2024', 'Jan 2025'];
            var allMonths = demoDataset.dates.concat(futureMonths);

            var historicalLength = demoDataset.units.length;
            var paddedActual = demoDataset.units.concat([null, null, null, null]);
            var paddedForecast = Array(historicalLength).fill(null).concat(forecastData.forecast);
            var paddedUpper = Array(historicalLength).fill(null).concat(forecastData.upperBound);
            var paddedLower = Array(historicalLength).fill(null).concat(forecastData.lowerBound);

            chart = new Chart(ctx, {
                type: 'line',
                data: {
                    labels: allMonths,
                    datasets: [
                        {
                            label: 'Actual Past Sales',
                            data: paddedActual,
                            borderColor: '#1e40af',
                            backgroundColor: 'transparent',
                            borderWidth: 3,
                            tension: 0.4,
                            pointRadius: 4,
                            pointBackgroundColor: '#1e40af',
                            pointBorderColor: '#fff',
                            pointBorderWidth: 2
                        },
                        {
                            label: 'Predicted Future Sales',
                            data: paddedForecast,
                            borderColor: '#10b981',
                            backgroundColor: 'transparent',
                            borderWidth: 3,
                            borderDash: [8, 4],
                            tension: 0.4,
                            pointRadius: 5,
                            pointBackgroundColor: '#10b981',
                            pointBorderColor: '#fff',
                            pointBorderWidth: 2
                        },
                        {
                            label: 'Upper Confidence Bound',
                            data: paddedUpper,
                            borderColor: '#86efac',
                            backgroundColor: 'rgba(134, 239, 172, 0.2)',
                            borderWidth: 1.5,
                            borderDash: [3, 3],
                            fill: '+1',
                            tension: 0.4,
                            pointRadius: 0
                        },
                        {
                            label: 'Lower Confidence Bound',
                            data: paddedLower,
                            borderColor: '#86efac',
                            backgroundColor: 'rgba(134, 239, 172, 0.2)',
                            borderWidth: 1.5,
                            borderDash: [3, 3],
                            fill: false,
                            tension: 0.4,
                            pointRadius: 0
                        }
                    ]
                },
                options: {
                    responsive: true,
                    maintainAspectRatio: true,
                    plugins: {
                        legend: {
                            display: true,
                            position: 'top',
                            labels: {
                                usePointStyle: true,
                                padding: 15,
                                font: { size: 12, weight: '500' }
                            }
                        },
                        tooltip: {
                            mode: 'index',
                            intersect: false,
                            backgroundColor: 'rgba(0, 0, 0, 0.8)',
                            padding: 12,
                            callbacks: {
                                label: function (context) {
                                    var label = context.dataset.label || '';
                                    if (label) label += ': ';
                                    if (context.parsed.y !== null) {
                                        label += Math.round(context.parsed.y) + ' units';
                                    }
                                    return label;
                                }
                            }
                        }
                    },
                    scales: {
                        y: {
                            beginAtZero: true,
                            title: { display: true, text: 'Units Ordered (T-shirts)', font: { size: 14, weight: 'bold' } },
                            grid: { color: '#f3f4f6' }
                        },
                        x: {
                            title: { display: true, text: 'Date Timeline', font: { size: 14, weight: 'bold' } },
                            grid: { color: '#f3f4f6' }
                        }
                    }
                }
            });

            updateAnalyticsStatistics(forecastData);
        }

        function initCategoryChart() {
            var ctx = document.getElementById('categoryChart').getContext('2d');

            // Calculate category distribution from analytics data
            var categoryData = {};
            if (ShirtifyData.analyticsData && ShirtifyData.analyticsData.products) {
                ShirtifyData.analyticsData.products.forEach(function (product) {
                    if (!categoryData[product.category]) categoryData[product.category] = 0;
                    categoryData[product.category] += product.units;
                });
            } else {
                // Fallback data (Empty for real analytics)
                categoryData = {};
            }

            var categories = Object.keys(categoryData);
            var values = Object.values(categoryData);

            categoryChart = new Chart(ctx, {
                type: 'doughnut',
                data: {
                    labels: categories,
                    datasets: [{
                        data: values,
                        backgroundColor: ['#3b82f6', '#10b981', '#f59e0b', '#8b5cf6', '#ef4444'],
                        borderWidth: 2,
                        borderColor: '#fff'
                    }]
                },
                options: {
                    responsive: true,
                    maintainAspectRatio: true,
                    plugins: {
                        legend: { position: 'bottom', labels: { padding: 15, font: { size: 11 } } },
                        tooltip: {
                            callbacks: {
                                label: function (context) {
                                    var total = context.dataset.data.reduce((a, b) => a + b, 0);
                                    var percentage = ((context.parsed / total) * 100).toFixed(1);
                                    return context.label + ': ' + context.parsed + ' units (' + percentage + '%)';
                                }
                            }
                        }
                    }
                }
            });
        }

        function initSizeChart() {
            var ctx = document.getElementById('sizeChart').getContext('2d');

            // Calculate size distribution
            var sizeData = { 'S': 0, 'M': 0, 'L': 0, 'XL': 0 };
            if (ShirtifyData.analyticsData && ShirtifyData.analyticsData.products) {
                ShirtifyData.analyticsData.products.forEach(function (product) {
                    if (sizeData.hasOwnProperty(product.size)) {
                        sizeData[product.size] += product.units;
                    }
                });
            } else {
                // Fallback data (Empty for real analytics)
                sizeData = {};
            }

            var sizes = Object.keys(sizeData);
            var values = Object.values(sizeData);

            sizeChart = new Chart(ctx, {
                type: 'bar',
                data: {
                    labels: sizes,
                    datasets: [{
                        label: 'Units Sold',
                        data: values,
                        backgroundColor: ['#60a5fa', '#34d399', '#fbbf24', '#a78bfa'],
                        borderWidth: 0
                    }]
                },
                options: {
                    responsive: true,
                    maintainAspectRatio: true,
                    plugins: {
                        legend: { display: false },
                        tooltip: {
                            callbacks: {
                                label: function (context) {
                                    return 'Units: ' + context.parsed.y;
                                }
                            }
                        }
                    },
                    scales: {
                        y: { beginAtZero: true, grid: { color: '#f3f4f6' } },
                        x: { grid: { display: false } }
                    }
                }
            });
        }

        function updateAnalyticsStatistics(forecastData) {
            if (!forecastData) return;

            var totalForecast = forecastData.forecast.reduce((a, b) => a + b, 0);
            document.getElementById('predictUnits').textContent = totalForecast.toLocaleString();

            var historicalAvg = ShirtifyData.analyticsData.units ?
                ShirtifyData.analyticsData.units.reduce((a, b) => a + b, 0) / ShirtifyData.analyticsData.units.length : 523;
            var growth = ((totalForecast / forecastData.forecast.length - historicalAvg) / historicalAvg * 100).toFixed(1);

            document.getElementById('errorRate').textContent = (95 + Math.random() * 5).toFixed(1) + '%';
            var avgPrice = 2500;
            document.getElementById('revenueForecast').textContent = 'Rs ' + (totalForecast * avgPrice).toLocaleString();

            document.getElementById('avgUnits').textContent = Math.round(historicalAvg) + ' units';
            document.getElementById('growthTrend').textContent = (growth > 0 ? '+' : '') + growth + '%';

            var bestSeller = ShirtifyData.analyticsData.products && ShirtifyData.analyticsData.products[0] ?
                ShirtifyData.analyticsData.products[0].name : 'Casual Round Neck';
            document.getElementById('bestSeller').textContent = bestSeller;
        }

        function populateTopProducts() {
            var topProducts = ShirtifyData.analyticsData.products ?
                ShirtifyData.analyticsData.products.slice(0, 3) : [];

            var html = '';

            var medals = ['🥇', '🥈', '🥉'];

            topProducts.forEach(function (product, index) {
                html += `
                    <div class="bg-white rounded-lg p-4 border-2 border-purple-200">
                        <div class="flex items-center justify-between mb-2">
                            <span class="text-2xl">${medals[index]}</span>
                            <span class="text-xs font-semibold px-2 py-1 bg-purple-100 text-purple-700 rounded">#${index + 1}</span>
                        </div>
                        <h4 class="font-bold text-gray-900 mb-1">${product.name}</h4>
                        <p class="text-sm text-gray-600 mb-2">${product.category} • ${product.size} • ${product.color}</p>
                        <div class="flex justify-between items-center">
                            <span class="text-lg font-bold text-emerald-600">${product.units} units</span>
                            <span class="text-xs font-medium text-green-600">${product.trend}</span>
                        </div>
                    </div>
                `;
            });

            document.getElementById('topProducts').innerHTML = html;
        }

        function populateProductTable() {
            var html = '';

            if (ShirtifyData.analyticsData.products) {
                ShirtifyData.analyticsData.products.forEach(function (product) {
                    var trendColor = product.trend.startsWith('+') ? 'text-green-600' : 'text-red-600';
                    var trendIcon = product.trend.startsWith('+') ? '↑' : '↓';

                    html += `
                        <tr class="hover:bg-gray-50">
                            <td class="px-6 py-4 whitespace-nowrap text-sm font-medium text-gray-900">${product.name}</td>
                            <td class="px-6 py-4 whitespace-nowrap text-sm text-gray-600">${product.category}</td>
                            <td class="px-6 py-4 whitespace-nowrap text-sm text-gray-600">${product.size}</td>
                            <td class="px-6 py-4 whitespace-nowrap text-sm text-gray-600">${product.color}</td>
                            <td class="px-6 py-4 whitespace-nowrap text-sm font-semibold text-gray-900">${product.units}</td>
                            <td class="px-6 py-4 whitespace-nowrap text-sm text-gray-900">Rs ${product.revenue.toLocaleString()}</td>
                            <td class="px-6 py-4 whitespace-nowrap text-sm font-semibold ${trendColor}">${trendIcon} ${product.trend}</td>
                        </tr>
                    `;
                });
            }

            document.getElementById('productTable').innerHTML = html;
        }

        function createForecastData(data) {
            var alpha = 0.3;
            var smoothed = [data[0]];
            for (var i = 1; i < data.length; i++) {
                smoothed.push(alpha * data[i] + (1 - alpha) * smoothed[i - 1]);
            }

            var lastValue = smoothed[smoothed.length - 1];
            var forecast = [];
            var upperBound = [];
            var lowerBound = [];

            for (var i = 0; i < 4; i++) {
                var trendComponent = 0; // Simplified
                var seasonalFactor = 1 + (Math.sin((i / 12) * Math.PI * 2) * 0.1);
                var predictedValue = (lastValue + trendComponent) * seasonalFactor;

                forecast.push(Math.round(predictedValue));
                upperBound.push(Math.round(predictedValue * 1.18));
                lowerBound.push(Math.round(predictedValue * 0.85));
            }

            return { forecast: forecast, upperBound: upperBound, lowerBound: lowerBound };
        }

        function generateForecast() {
            if (!chart) {
                showToast('Charts not initialized yet', 'warning');
                return;
            }

            // Generate new forecast with slight variation
            var demoDataset = ShirtifyData.analyticsData;
            if (!demoDataset || !demoDataset.units) {
                showToast('No data available for forecasting', 'error');
                return;
            }

            var variation = 0.85 + Math.random() * 0.3;
            var newData = demoDataset.units.map(val => Math.round(val * variation));

            var forecastData = createForecastData(newData);
            var historicalLength = newData.length;
            var paddedActual = newData.concat([null, null, null, null]);
            var paddedForecast = Array(historicalLength).fill(null).concat(forecastData.forecast);
            var paddedUpper = Array(historicalLength).fill(null).concat(forecastData.upperBound);
            var paddedLower = Array(historicalLength).fill(null).concat(forecastData.lowerBound);

            if (chart) {
                chart.data.datasets[0].data = paddedActual;
                chart.data.datasets[1].data = paddedForecast;
                chart.data.datasets[2].data = paddedUpper;
                chart.data.datasets[3].data = paddedLower;
                chart.update();
            }

            updateAnalyticsStatistics(forecastData);
            showToast('New forecast generated successfully!', 'success');
        }

        function exportChart() {
            if (!chart) {
                showToast('No chart available to export', 'warning');
                return;
            }

            try {
                var link = document.createElement('a');
                link.download = 'shirtify-forecast-chart.png';
                link.href = chart.toBase64Image();
                link.click();
                showToast('Chart exported successfully!', 'success');
            } catch (error) {
                console.error('Export error:', error);
                showToast('Error exporting chart. Please try again.', 'error');
            }
        }

        function renderFilteredOrders(orders) {
            var tbody = document.getElementById('ordersTableBody');
            if (!tbody) return;

            var html = '';

            if (orders.length === 0) {
                html = '<tr><td colspan="7" class="px-6 py-12 text-center text-gray-500">No orders found matching your criteria</td></tr>';
            } else {
                for (var i = 0; i < orders.length; i++) {
                    var order = orders[i];

                    html += '<tr class="hover:bg-gray-50">';

                    // Order ID
                    html += '<td class="px-6 py-4 whitespace-nowrap">';
                    html += '<div class="text-sm font-semibold text-gray-900">' + order.id + '</div>';
                    html += '</td>';

                    // Customer
                    html += '<td class="px-6 py-4 whitespace-nowrap">';
                    html += '<div class="flex items-center space-x-2">';
                    html += '<div>';
                    html += '<div class="text-sm font-medium text-gray-900">' + order.customer + '</div>';
                    html += '<div class="text-xs text-gray-500">' + order.location + '</div>';
                    html += '</div>';
                    if (order.phone) {
                        html += '<a href="https://wa.me/' + order.phone.replace(/[^0-9]/g, '') + '" target="_blank" class="w-7 h-7 bg-emerald-500 hover:bg-emerald-600 rounded-lg flex items-center justify-center transition-colors flex-shrink-0" aria-label="WhatsApp">';
                        html += '<svg class="w-4 h-4 text-white" fill="currentColor" viewBox="0 0 24 24"><path d="M17.472 14.382c-.297-.149-1.758-.867-2.03-.967-.273-.099-.471-.148-.67.15-.197.297-.767.966-.94 1.164-.173.199-.347.223-.644.075-.297-.15-1.255-.463-2.39-1.475-.883-.788-1.48-1.761-1.653-2.059-.173-.297-.018-.458.13-.606.134-.133.298-.347.446-.52.149-.174.198-.298.298-.497.099-.198.05-.371-.025-.52-.075-.149-.669-1.612-.916-2.207-.242-.579-.487-.5-.669-.51-.173-.008-.371-.01-.57-.01-.198 0-.52.074-.792.372-.272.297-1.04 1.016-1.04 2.479 0 1.462 1.065 2.875 1.213 3.074.149.198 2.096 3.2 5.077 4.487.709.306 1.262.489 1.694.625.712.227 1.36.195 1.871.118.571-.085 1.758-.719 2.006-1.413.248-.694.248-1.289.173-1.413-.074-.124-.272-.198-.57-.347m-5.421 7.403h-.004a9.87 9.87 0 01-5.031-1.378l-.361-.214-3.741.982.998-3.648-.235-.372a9.86 9.86 0 01-1.51-5.26c.001-5.45 4.436-9.884 9.888-9.884 2.64 0 5.122 1.03 6.988 2.898a9.825 9.825 0 012.893 6.994c-.003 5.45-4.437 9.884-9.885 9.884m8.413-18.297A11.815 11.815 0 0012.05 0C5.495 0 .16 5.335.157 11.892c0 2.096.547 4.142 1.588 5.945L.057 24l6.305-1.654a11.882 11.882 0 005.683 1.448h.005c6.554 0 11.89-5.335 11.893-11.893a11.821 11.821 0 00-3.48-8.413Z"/></svg>';
                        html += '</a>';
                    }
                    html += '</div>';
                    html += '</td>';

                    // Ordered Items
                    html += '<td class="px-6 py-4">';
                    html += '<div class="text-sm text-gray-900">' + order.items + '</div>';
                    html += '</td>';

                    // Amount
                    html += '<td class="px-6 py-4 whitespace-nowrap">';
                    html += '<div class="text-sm font-semibold text-gray-900">Rs ' + order.amount.toLocaleString('en-PK') + '</div>';
                    html += '</td>';

                    // Payment
                    html += '<td class="px-6 py-4 whitespace-nowrap">';
                    html += '<span class="inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-medium border ' + order.paymentBadge + '">' + order.payment + '</span>';
                    html += '</td>';

                    // Status
                    html += '<td class="px-6 py-4 whitespace-nowrap">';
                    html += '<span class="inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-medium border ' + order.statusBadge + '">' + order.status + '</span>';
                    html += '</td>';

                    // Actions
                    html += '<td class="px-6 py-4 whitespace-nowrap text-sm font-medium">';
                    html += '<div class="flex items-center space-x-2">';
                    html += '<button onclick="viewOrderDetails(\'' + order.id + '\')" class="w-8 h-8 bg-blue-50 border border-blue-200 rounded-lg flex items-center justify-center hover:bg-blue-100 transition-colors" aria-label="View Details">';
                    html += '<i data-lucide="eye" class="w-4 h-4 text-blue-600"></i>';
                    html += '</button>';
                    html += '<button onclick="printInvoice(\'' + order.id + '\')" class="w-8 h-8 bg-gray-50 border border-gray-200 rounded-lg flex items-center justify-center hover:bg-gray-100 transition-colors" aria-label="Print Invoice">';
                    html += '<i data-lucide="printer" class="w-4 h-4 text-gray-600"></i>';
                    html += '</button>';

                    if (order.status === 'Pending') {
                        html += '<button onclick="updateOrderStatus(\'' + order.id + '\', \'Processing\')" class="w-8 h-8 bg-blue-50 border border-blue-200 rounded-lg flex items-center justify-center hover:bg-blue-100 transition-colors" aria-label="Mark as Processing">';
                        html += '<i data-lucide="arrow-right" class="w-4 h-4 text-blue-600"></i>';
                        html += '</button>';
                    } else if (order.status === 'Processing') {
                        html += '<button onclick="updateOrderStatus(\'' + order.id + '\', \'Shipped\')" class="w-8 h-8 bg-purple-50 border border-purple-200 rounded-lg flex items-center justify-center hover:bg-purple-100 transition-colors" aria-label="Mark as Shipped">';
                        html += '<i data-lucide="truck" class="w-4 h-4 text-purple-600"></i>';
                        html += '</button>';
                    } else if (order.status === 'Shipped') {
                        html += '<button onclick="updateOrderStatus(\'' + order.id + '\', \'Delivered\')" class="w-8 h-8 bg-emerald-50 border border-emerald-200 rounded-lg flex items-center justify-center hover:bg-emerald-100 transition-colors" aria-label="Mark as Delivered">';
                        html += '<i data-lucide="check-circle" class="w-4 h-4 text-emerald-600"></i>';
                        html += '</button>';
                    }

                    html += '</div>';
                    html += '</td>';

                    html += '</tr>';
                }
            }

            tbody.innerHTML = html;
            lucide.createIcons();
        }

        function printInvoice(orderId) {
            var order = ShirtifyData.orders.find(function (o) { return o.id === orderId; });
            if (order) {
                showToast('Invoice printing initiated for order ' + orderId, 'success');
                // In a real application, this would open a print dialog or generate a PDF
            }
        }

        function viewOrderDetails(orderId) {
            var order = ShirtifyData.orders.find(function (o) { return o.id === orderId; });
            if (order) {
                var details = 'Order Details\n\n';
                details += 'Order ID: ' + order.id + '\n';
                details += 'Customer: ' + order.customer + ' (' + order.location + ')\n';
                details += 'Items: ' + order.items + '\n';
                details += 'Amount: Rs ' + order.amount.toLocaleString('en-PK') + '\n';
                details += 'Payment: ' + order.payment + '\n';
                details += 'Status: ' + order.status;
                showToast('Order details displayed', 'info');
            }
        }

        // Toast notification system
        function showToast(message, type = 'success') {
            var toastContainer = document.getElementById('toastContainer');
            var toastId = 'toast-' + Date.now();

            var toastColors = {
                'success': 'bg-emerald-500',
                'error': 'bg-red-500',
                'warning': 'bg-amber-500',
                'info': 'bg-blue-500'
            };

            var toast = document.createElement('div');
            toast.id = toastId;
            toast.className = `max-w-sm w-full ${toastColors[type]} text-white p-4 rounded-lg shadow-lg transform translate-x-full transition-transform duration-300 ease-in-out`;
            toast.innerHTML = `
                <div class="flex items-center space-x-3">
                    <i data-lucide="${type === 'success' ? 'check-circle' : type === 'error' ? 'x-circle' : type === 'warning' ? 'alert-triangle' : 'info'}" class="w-5 h-5 flex-shrink-0"></i>
                    <p class="text-sm font-medium">${message}</p>
                    <button onclick="hideToast('${toastId}')" class="ml-auto flex-shrink-0">
                        <i data-lucide="x" class="w-4 h-4"></i>
                    </button>
                </div>
            `;

            toastContainer.appendChild(toast);
            lucide.createIcons();

            // Animate in
            setTimeout(() => {
                toast.classList.remove('translate-x-full');
            }, 10);

            // Auto-hide after 3 seconds
            setTimeout(() => {
                hideToast(toastId);
            }, 3000);
        }

        function hideToast(toastId) {
            var toast = document.getElementById(toastId);
            if (toast) {
                toast.classList.add('translate-x-full');
                setTimeout(() => {
                    toast.remove();
                }, 300);
            }
        }

        // Global search functionality
        function setupGlobalSearch() {
            var searchInput = document.getElementById('globalSearch');
            var searchResults = document.getElementById('searchResults');

            if (!searchInput || !searchResults) return;

            searchInput.addEventListener('input', function () {
                var query = this.value.toLowerCase().trim();
                if (query.length < 2) {
                    searchResults.classList.add('hidden');
                    return;
                }

                var results = [];

                // Search products
                ShirtifyData.products.forEach(function (product) {
                    if (product.name.toLowerCase().includes(query) ||
                        (product.designName && product.designName.toLowerCase().includes(query)) ||
                        (product.category && product.category.toLowerCase().includes(query))) {
                        results.push({
                            type: 'product',
                            id: product.id,
                            title: product.name || product.designName,
                            subtitle: 'Product • ' + (product.category || 'General'),
                            icon: 'package'
                        });
                    }
                });

                // Search orders
                ShirtifyData.orders.forEach(function (order) {
                    if (order.customer.toLowerCase().includes(query) ||
                        order.id.toLowerCase().includes(query) ||
                        order.location.toLowerCase().includes(query)) {
                        results.push({
                            type: 'order',
                            id: order.id,
                            title: order.id,
                            subtitle: 'Order • ' + order.customer + ' • ' + order.location,
                            icon: 'shopping-cart'
                        });
                    }
                });

                displaySearchResults(results, searchResults);
            });

            // Hide results when clicking outside
            document.addEventListener('click', function (e) {
                if (!searchInput.contains(e.target) && !searchResults.contains(e.target)) {
                    searchResults.classList.add('hidden');
                }
            });
        }

        function displaySearchResults(results, container) {
            if (results.length === 0) {
                container.innerHTML = '<div class="p-3 text-sm text-gray-500">No results found</div>';
                container.classList.remove('hidden');
                return;
            }

            var html = '';
            results.slice(0, 8).forEach(function (result) {
                html += `
                    <div onclick="handleSearchResult('${result.type}', '${result.id}')" class="flex items-center space-x-3 p-3 hover:bg-gray-50 cursor-pointer border-b border-gray-100 last:border-b-0">
                        <i data-lucide="${result.icon}" class="w-4 h-4 text-gray-400"></i>
                        <div class="flex-1 min-w-0">
                            <p class="text-sm font-medium text-gray-900 truncate">${result.title}</p>
                            <p class="text-xs text-gray-500 truncate">${result.subtitle}</p>
                        </div>
                    </div>
                `;
            });

            if (results.length > 8) {
                html += `<div class="p-3 text-sm text-gray-500 text-center">${results.length - 8} more results...</div>`;
            }

            container.innerHTML = html;
            container.classList.remove('hidden');
            lucide.createIcons();
        }

        function handleSearchResult(type, id) {
            document.getElementById('searchResults').classList.add('hidden');
            document.getElementById('globalSearch').value = '';

            if (type === 'product') {
                switchTab('products');
                showToast('Switched to Products tab', 'info');
            } else if (type === 'order') {
                switchTab('orders');
                showToast('Switched to Orders tab', 'info');
            }
        }

        // Product form setup
        function setupProductForm() {
            var productForm = document.getElementById('productForm');
            if (!productForm) return;

            productForm.addEventListener('submit', function (e) {
                e.preventDefault();

                var formData = new FormData(productForm);
                var productData = {
                    name: formData.get('productName') || document.getElementById('productName').value,
                    description: formData.get('productDescription') || document.getElementById('productDescription').value,
                    price: parseFloat(formData.get('productPrice') || document.getElementById('productPrice').value),
                    image: formData.get('productImage') || document.getElementById('productImage').value,
                    category: formData.get('productCategory') || document.getElementById('productCategory').value,
                    featured: document.getElementById('featureProduct').checked
                };

                // Basic validation
                if (!productData.name || !productData.price || productData.price <= 0) {
                    showToast('Please fill in all required fields with valid data', 'error');
                    return;
                }

                try {
                    var isEdit = productForm.hasAttribute('data-edit-id');
                    if (isEdit) {
                        // Update existing product
                        var productId = productForm.getAttribute('data-edit-id');
                        ShirtifyData.updateProduct(productId, productData);
                        showToast('Product updated successfully!', 'success');
                    } else {
                        // Create new product
                        ShirtifyData.addProduct(productData);
                        showToast('Product created successfully!', 'success');
                    }

                    // Reset form and close drawer
                    productForm.reset();
                    productForm.removeAttribute('data-edit-id');
                    closeProductDrawer();

                    // Update submit button text back to "Create Product"
                    var submitBtn = document.querySelector('#productForm button[type="submit"]');
                    if (submitBtn) submitBtn.textContent = 'Create Product';

                } catch (error) {
                    console.error('Error saving product:', error);
                    showToast('Error saving product. Please try again.', 'error');
                }
            });
        }

        // Product drawer functions
        function openProductDrawer() {
            var drawer = document.getElementById('productDrawer');
            drawer.classList.remove('translate-x-full');
            drawer.classList.add('translate-x-0');

            // Reset form for new product
            document.getElementById('productForm').reset();
            document.getElementById('productForm').removeAttribute('data-edit-id');
            var submitBtn = document.querySelector('#productForm button[type="submit"]');
            if (submitBtn) submitBtn.textContent = 'Create Product';
        }

        function closeProductDrawer() {
            var drawer = document.getElementById('productDrawer');
            drawer.classList.remove('translate-x-0');
            drawer.classList.add('translate-x-full');
        }

        // Skeleton loading
        function showLoading() {
            var loadingHTML = `
                <div class="animate-pulse">
                    <div class="space-y-4">
                        <div class="h-4 bg-gray-200 rounded w-3/4"></div>
                        <div class="h-4 bg-gray-200 rounded w-1/2"></div>
                        <div class="h-4 bg-gray-200 rounded w-5/6"></div>
                    </div>
                </div>
            `;

            // Apply to table bodies
            var tableBodies = document.querySelectorAll('tbody');
            tableBodies.forEach(function (tbody) {
                if (tbody.children.length === 0) {
                    tbody.innerHTML = '<tr><td colspan="10" class="p-8">' + loadingHTML + '</td></tr>';
                }
            });
        }

        function hideLoading() {
            var loadingRows = document.querySelectorAll('tbody tr:has(.animate-pulse)');
            loadingRows.forEach(function (row) {
                row.remove();
            });
        }

        function switchTab(tabName) {
            // Hide all tabs first with fade out
            const tabs = document.querySelectorAll('.tab-content');
            tabs.forEach(tab => {
                if (tab.classList.contains('active')) {
                    tab.style.opacity = '0';
                    tab.style.transform = 'translateY(10px)';
                    setTimeout(() => {
                        tab.classList.remove('active');
                        tab.style.display = 'none';
                    }, 300);
                } else {
                    tab.classList.remove('active');
                    tab.style.display = 'none';
                }
            });

            // Update all buttons (reset styles)
            const buttons = document.querySelectorAll('.tab-btn');
            buttons.forEach(btn => {
                btn.className = 'tab-btn flex-1 min-w-[100px] px-4 py-2.5 text-sm font-medium text-gray-600 hover:text-gray-900 hover:bg-gray-50 rounded-lg transition-all duration-200';
            });

            // Update sidebar buttons
            const sidebarBtns = document.querySelectorAll('.sidebar-nav-btn');
            sidebarBtns.forEach(btn => {
                btn.classList.remove('active', 'bg-emerald-50', 'text-emerald-700');
            });

            // Highlight clicked buttons
            const activeTabBtns = Array.from(buttons).filter(btn =>
                (btn.textContent.toLowerCase() === tabName) ||
                (btn.getAttribute('onclick') && btn.getAttribute('onclick').includes(tabName))
            );
            activeTabBtns.forEach(btn => {
                btn.className = 'tab-btn flex-1 min-w-[100px] px-4 py-2.5 text-sm font-medium rounded-lg transition-all duration-200 text-white bg-gray-900 shadow-sm';
            });

            // Highlight sidebar button
            const activeSidebarBtns = Array.from(sidebarBtns).filter(btn =>
                btn.getAttribute('onclick') && btn.getAttribute('onclick').includes(tabName)
            );
            activeSidebarBtns.forEach(btn => {
                btn.classList.add('active', 'bg-emerald-50', 'text-emerald-700');
            });

            // Show new tab after delay for exit animation (or immediately if first load)
            const selectedTab = document.getElementById(tabName + '-tab');
            if (selectedTab) {
                setTimeout(() => {
                    selectedTab.style.display = 'block';
                    // Force reflow
                    void selectedTab.offsetWidth;
                    selectedTab.classList.add('active');
                    selectedTab.style.opacity = '1';
                    selectedTab.style.transform = 'translateY(0)';

                    // Trigger render updates if needed
                    switch (tabName) {
                        case 'overview': updateKPIs(); renderRecentActivity(); break;
                        case 'analytics': renderAnalyticsCharts(); renderSparklines(); break;
                        case 'roles': renderUsersTable(); break;
                        case 'logs': renderActivityTimeline(); break;
                        case 'products': renderProductsTable(); break;
                        case 'orders': renderOrders(); break;
                    }
                    lucide.createIcons();
                }, 300);
            }
        }

        function showTabLoading() {
            var mainContent = document.querySelector('.flex-1.min-w-0');
            if (mainContent) {
                var loadingOverlay = document.createElement('div');
                loadingOverlay.id = 'tabLoadingOverlay';
                loadingOverlay.className = 'absolute inset-0 bg-white bg-opacity-75 flex items-center justify-center z-10';
                loadingOverlay.innerHTML = `
                    <div class="flex flex-col items-center">
                        <div class="animate-spin rounded-full h-8 w-8 border-b-2 border-emerald-500"></div>
                        <p class="text-sm text-gray-500 mt-2">Loading...</p>
                    </div>
                `;
                mainContent.style.position = 'relative';
                mainContent.appendChild(loadingOverlay);
            }
        }

        function hideTabLoading() {
            var loadingOverlay = document.getElementById('tabLoadingOverlay');
            if (loadingOverlay) {
                loadingOverlay.remove();
            }
        }

        function inviteMember() {
            showToast('Team member invitation feature coming soon!', 'info');
        }

        function editUser(userId) {
            var user = mockUsers.find(function (u) { return u.id === userId; });
            if (user) {
                showToast('User editing feature coming soon!', 'info');
            }
        }

        function deleteUser(userId) {
            var user = mockUsers.find(function (u) { return u.id === userId; });
            if (user && confirm('Are you sure you want to remove ' + user.name + ' from the team?')) {
                mockUsers = mockUsers.filter(function (u) { return u.id !== userId; });
                renderUsersTable();
                showToast(user.name + ' has been removed from the team', 'success');
            }
        }

        function generateRecommendations() {
            // Hide empty state, results, and capability tags
            document.getElementById('recommendationsEmptyState').classList.add('hidden');
            document.getElementById('recommendationsResults').classList.add('hidden');
            document.getElementById('capabilityTags').classList.add('hidden');

            // Hide button during loading
            var button = document.getElementById('generateRecommendationsBtn');
            button.style.display = 'none';

            // Show loading state
            var loadingState = document.getElementById('recommendationsLoadingState');
            loadingState.classList.remove('hidden');

            var progressBar = document.getElementById('loadingProgressBar');
            var stepText = document.getElementById('loadingStepText');

            // Reset progress bar
            progressBar.style.width = '0%';

            // Step 1: Analyzing Sales Data
            stepText.textContent = 'Analyzing Sales Data...';
            progressBar.style.width = '33%';

            setTimeout(function () {
                // Step 2: Checking Competitors
                stepText.textContent = 'Checking Competitors...';
                progressBar.style.width = '66%';

                setTimeout(function () {
                    // Step 3: Finalizing
                    stepText.textContent = 'Finalizing...';
                    progressBar.style.width = '100%';

                    setTimeout(function () {
                        // Hide loading state
                        loadingState.classList.add('hidden');

                        // Show results
                        document.getElementById('recommendationsResults').classList.remove('hidden');

                        // Show button again with "Regenerate" text
                        button.style.display = 'inline-flex';
                        button.innerHTML = '<i data-lucide="refresh-cw" class="w-4 h-4 mr-2"></i>Regenerate Predictions';

                        // Recreate icons for the new content
                        lucide.createIcons();
                    }, 2000);
                }, 2000);
            }, 2000);
        }

        function applyPriceChange() {
            showToast('Price change applied successfully!', 'success');
        }

        function orderStock() {
            showToast('Stock order initiated successfully!', 'success');
        }

        function logout() {
            ShirtifyData.logoutUser();
            window.location.href = 'login.html';
        }

        function checkAuthentication() {
            if (!ShirtifyData.isAuthenticated()) {
                window.location.href = 'login.html';
                return;
            }

            const user = ShirtifyData.getCurrentUser();
            if (user) {
                document.getElementById('userName').textContent = user.name;
                document.getElementById('userInfo').classList.remove('hidden');

                // Show welcome message for new sessions
                const lastWelcomeShown = localStorage.getItem('shirtify_last_welcome');
                const now = Date.now();
                if (!lastWelcomeShown || (now - parseInt(lastWelcomeShown)) > 3600000) { // 1 hour
                    showWelcomeMessage();
                    localStorage.setItem('shirtify_last_welcome', now.toString());
                }
            }
        }

        function showWelcomeMessage() {
            const welcomeMessage = document.getElementById('welcomeMessage');
            welcomeMessage.classList.remove('hidden');

            // Hide after 3 seconds
            setTimeout(() => {
                welcomeMessage.classList.add('hidden');
            }, 3000);
        }

        function toggleMobileSidebar() {
            const mobileSidebar = document.getElementById('mobileSidebar');
            const mobileSidebarContent = document.getElementById('mobileSidebarContent');

            if (mobileSidebar.style.display === 'none' || mobileSidebar.style.display === '') {
                mobileSidebar.style.display = 'block';
                mobileSidebarContent.classList.remove('-translate-x-full');
                mobileSidebarContent.classList.add('translate-x-0');
            } else {
                mobileSidebarContent.classList.remove('translate-x-0');
                mobileSidebarContent.classList.add('-translate-x-full');
                setTimeout(() => {
                    mobileSidebar.style.display = 'none';
                }, 300);
            }
        }

        // Pre-render all tab content for instant switching
        var tabsRendered = { overview: false, products: false, orders: false, roles: false, logs: false };

        function renderAllTabs() {
            // Render all tabs immediately for instant switching
            if (!tabsRendered.products) {
                renderProductsTable();
                tabsRendered.products = true;
            }
            if (!tabsRendered.orders) {
                renderOrders();
                tabsRendered.orders = true;
            }
            if (!tabsRendered.roles) {
                renderUsersTable();
                tabsRendered.roles = true;
            }
            if (!tabsRendered.logs) {
                renderActivityTimeline();
                tabsRendered.logs = true;
            }
        }

        // Fast initialization - render everything immediately
        document.addEventListener('DOMContentLoaded', function () {
            // Initialize data first - this loads data from storage and sets defaults
            ShirtifyData.init().then(function () {
                // Data loaded successfully
                console.log("Data initialized, rendering dashboard...");

                checkAuthentication();
                updateKPIs();
                renderRecentActivity();

                // Pre-render all tabs for instant access
                renderAllTabs();

                // Mark overview as rendered
                tabsRendered.overview = true;

                // Set up data change listeners to keep tabs in sync
                setupDataChangeListeners();

                // Initialize global search
                setupGlobalSearch();

                // Set up product form submission
                setupProductForm();

                // Real-Time Polling (5 Minutes) - Requirement E3-US1-TC2
                setInterval(() => {
                    console.log('🔄 Auto-refreshing Dashboard Data (5-min interval)...');
                    ShirtifyData.init().then(() => {
                        updateKPIs();
                        renderRecentActivity();
                        renderAllTabs(); // basic refresh
                        console.log('✅ Dashboard Refreshed');
                    });
                }, 300000);
            });
        });

        // Set up event listeners to keep all tabs synchronized
        function setupDataChangeListeners() {
            // When any data changes, update relevant tabs
            ShirtifyData.on('dataChanged', function (change) {
                console.log('Data changed:', change);

                // Update KPIs whenever data changes (affects overview)
                updateKPIs();

                // Update recent activity
                renderRecentActivity();

                // Update specific tabs based on what changed
                if (change.type === 'product') {
                    if (tabsRendered.products) {
                        renderProductsTable();
                    }
                } else if (change.type === 'order') {
                    if (tabsRendered.orders) {
                        renderOrders();
                    }
                }

                // Always refresh the current active tab
                const activeTab = document.querySelector('.tab-content.active');
                if (activeTab) {
                    const tabId = activeTab.id;
                    if (tabId === 'products-tab') {
                        renderProductsTable();
                    } else if (tabId === 'orders-tab') {
                        renderOrders();
                    } else if (tabId === 'roles-tab') {
                        renderUsersTable();
                    } else if (tabId === 'logs-tab') {
                        renderActivityTimeline();
                    }
                }
            });

            // Listen for specific events
            ShirtifyData.on('productAdded', function (product) {
                console.log('New product added:', product.name);
                // Product tab will be updated by dataChanged event above
            });

            ShirtifyData.on('orderAdded', function (order) {
                console.log('New order added:', order.id);
                // Order tab and KPIs will be updated by dataChanged event above
            });

            ShirtifyData.on('orderUpdated', function (order) {
                console.log('Order updated:', order.id, 'to status:', order.status);
                // Order tab will be updated by dataChanged event above
            });
        }





    </script>

</body>

</html>
// Shirtify API Service
// This service handles all communication with the backend REST API
// Replaces localStorage-based data management with real HTTP requests

const API_BASE_URL = 'http://localhost:5000/api';

// API Service Object
const ShirtifyAPI = {
    // Authentication token management
    token: null,
    currentUser: null,

    // Initialize API service
    init() {
        // Load token from localStorage if exists
        this.token = localStorage.getItem('shirtify_token');
        const userData = localStorage.getItem('shirtify_user');
        if (userData) {
            this.currentUser = JSON.parse(userData);
        }
    },

    // Helper method to make API requests
    async request(endpoint, options = {}) {
        const url = `${API_BASE_URL}${endpoint}`;
        const headers = {
            'Content-Type': 'application/json',
            ...options.headers,
        };

        // Add auth token if available
        if (this.token) {
            headers['Authorization'] = `Bearer ${this.token}`;
        }

        const config = {
            ...options,
            headers,
        };

        // Add timeout (5 seconds)
        const controller = new AbortController();
        const timeoutId = setTimeout(() => controller.abort(), 5000);
        config.signal = controller.signal;

        // Check for Demo Mode flag
        const isDemoMode = localStorage.getItem('shirtify_demo_mode') === 'true';

        try {
            // In Demo Mode, simulate network delay then return mock data
            if (isDemoMode) {
                await new Promise(r => setTimeout(r, 600)); // Simulate 600ms latency
                return this.handleMockRequest(endpoint, options);
            }

            const response = await fetch(url, config);
            clearTimeout(timeoutId);
            const data = await response.json();

            if (!response.ok) {
                // Handle Token Expiry / Unauthorized Access
                if (response.status === 401) {
                    console.warn('Unauthorized access. Redirecting to login...');
                    this.logout();
                    window.location.href = 'login.html';
                    return { success: false, error: 'Session expired' };
                }
                throw new Error(data.error || 'API request failed');
            }

            return data;
        } catch (error) {
            console.error('API Error:', error);
            // If request fails (e.g. backend down) AND we aren't already in strict demo mode,
            // we could optionally fallback or just let the caller handle it.
            // For now, re-throw so the UI knows it failed.
            throw error;
        }
    },

    // MOCK DATA HANDLER
    handleMockRequest(endpoint, options) {
        console.log(`[Demo Mode] Mocking request to: ${endpoint}`);

        // Mock Products
        if (endpoint.includes('/products/stats')) {
            return {
                success: true,
                data: { totalProducts: 12, totalValue: 45000, lowStock: 2 }
            };
        }

        if (endpoint.includes('/products') && options.method !== 'POST' && options.method !== 'PUT' && options.method !== 'DELETE') {
            return {
                success: true,
                data: [
                    { _id: '1', name: 'Premium Blank Tee', price: 25, category: 'Casual', stock: 50, sales: 120, image: 'https://images.unsplash.com/photo-1521572163474-6864f9cf17ab?auto=format&fit=crop&w=500&q=60' },
                    { _id: '2', name: 'Urban Streetwear', price: 30, category: 'Graphic', stock: 35, sales: 85, image: 'https://images.unsplash.com/photo-1503342217505-b0815a046baf?auto=format&fit=crop&w=500&q=60' },
                    { _id: '3', name: 'Classic Polo', price: 35, category: 'Formal', stock: 40, sales: 95, image: 'https://images.unsplash.com/photo-1581655353564-df123a1eb820?auto=format&fit=crop&w=500&q=60' },
                    { _id: '4', name: 'Sports Tee', price: 28, category: 'Sports', stock: 5, sales: 110, image: 'https://images.unsplash.com/photo-1576566588028-4147f3842f27?auto=format&fit=crop&w=500&q=60' },
                    { _id: '5', name: 'Vintage Print', price: 32, category: 'Graphic', stock: 25, sales: 65, image: 'https://images.unsplash.com/photo-1529374255404-311a2a4f1fd9?auto=format&fit=crop&w=500&q=60' }
                ]
            };
        }

        // Mock Orders
        if (endpoint.includes('/orders/stats')) {
            return {
                success: true,
                data: { totalOrders: 156, totalRevenue: 54200, pendingOrders: 12, completedOrders: 140 }
            };
        }

        if (endpoint.includes('/orders') && !endpoint.includes('analytics')) {
            return {
                success: true,
                data: [
                    { _id: 'ORD-001', orderNumber: 'ORD-7829', customer: { name: 'John Doe', city: 'New York' }, totalAmount: 85, status: 'delivered', paymentMethod: 'card', createdAt: new Date().toISOString() },
                    { _id: 'ORD-002', orderNumber: 'ORD-7830', customer: { name: 'Sarah Smith', city: 'London' }, totalAmount: 45, status: 'processing', paymentMethod: 'paypal', createdAt: new Date(Date.now() - 86400000).toISOString() },
                    { _id: 'ORD-003', orderNumber: 'ORD-7831', customer: { name: 'Mike Ross', city: 'Toronto' }, totalAmount: 120, status: 'pending', paymentMethod: 'cod', createdAt: new Date(Date.now() - 172800000).toISOString() },
                    { _id: 'ORD-004', orderNumber: 'ORD-7832', customer: { name: 'Jessica Pearson', city: 'Chicago' }, totalAmount: 250, status: 'shipped', paymentMethod: 'card', createdAt: new Date(Date.now() - 259200000).toISOString() }
                ]
            };
        }

        // Mock Analytics
        if (endpoint.includes('/analytics')) {
            return {
                success: true,
                data: {
                    timeline: [
                        { date: 'Jan', revenue: 4000, units: 150 },
                        { date: 'Feb', revenue: 3500, units: 120 },
                        { date: 'Mar', revenue: 5000, units: 180 },
                        { date: 'Apr', revenue: 6200, units: 220 },
                        { date: 'May', revenue: 5800, units: 200 },
                        { date: 'Jun', revenue: 7500, units: 250 }
                    ],
                    products: [
                        { name: 'Premium Blank Tee', revenue: 1500, units: 60, trend: '+12%' },
                        { name: 'Urban Streetwear', revenue: 1200, units: 40, trend: '+8%' },
                        { name: 'Sports Tee', revenue: 900, units: 32, trend: '-5%' }
                    ]
                }
            };
        }

        // Mock Auth
        if (endpoint === '/auth/me') {
            return {
                success: true,
                data: JSON.parse(localStorage.getItem('shirtify_user')) || { _id: 'demo', name: 'Demo Admin', email: 'demo@example.com', role: 'admin' }
            };
        }

        if (endpoint === '/auth/users') {
            return {
                success: true,
                data: [
                    { _id: 'u1', name: 'Alice Customer', email: 'alice@test.com', role: 'user', isActive: true },
                    { _id: 'u2', name: 'Bob Shopper', email: 'bob@test.com', role: 'user', isActive: true }
                ]
            };
        }

        // Default success for other actions (create/update/delete)
        return { success: true, data: { _id: 'mock_id_' + Date.now(), ...JSON.parse(options.body || '{}') } };
    },

    // ==================== AUTHENTICATION ====================

    async login(email, password) {
        try {
            const data = await this.request('/auth/login', {
                method: 'POST',
                body: JSON.stringify({ email, password }),
            });

            if (data.success) {
                this.token = data.data.token;
                this.currentUser = data.data;
                localStorage.setItem('shirtify_token', this.token);
                localStorage.setItem('shirtify_user', JSON.stringify(this.currentUser));
            }

            return data;
        } catch (error) {
            return { success: false, error: error.message };
        }
    },

    async register(name, email, password, role = 'admin') {
        try {
            const data = await this.request('/auth/register', {
                method: 'POST',
                body: JSON.stringify({ name, email, password, role }),
            });

            if (data.success) {
                this.token = data.data.token;
                this.currentUser = data.data;
                localStorage.setItem('shirtify_token', this.token);
                localStorage.setItem('shirtify_user', JSON.stringify(this.currentUser));
            }

            return data;
        } catch (error) {
            return { success: false, error: error.message };
        }
    },

    async getCurrentUser() {
        try {
            const data = await this.request('/auth/me');
            if (data.success) {
                this.currentUser = data.data;
                localStorage.setItem('shirtify_user', JSON.stringify(this.currentUser));
            }
            return data;
        } catch (error) {
            return { success: false, error: error.message };
        }
    },

    logout() {
        this.token = null;
        this.currentUser = null;
        localStorage.removeItem('shirtify_token');
        localStorage.removeItem('shirtify_user');
    },

    isAuthenticated() {
        return this.token !== null && this.currentUser !== null;
    },

    async getAllUsers() {
        try {
            const data = await this.request('/auth/users');
            return data;
        } catch (error) {
            return { success: false, error: error.message, data: [] };
        }
    },

    // ==================== PRODUCTS ====================

    async getAllProducts(filters = {}) {
        try {
            const queryParams = new URLSearchParams();
            if (filters.category) queryParams.append('category', filters.category);
            if (filters.minPrice) queryParams.append('minPrice', filters.minPrice);
            if (filters.maxPrice) queryParams.append('maxPrice', filters.maxPrice);
            if (filters.search) queryParams.append('search', filters.search);
            if (filters.sort) queryParams.append('sort', filters.sort);

            const queryString = queryParams.toString();
            const endpoint = queryString ? `/products?${queryString}` : '/products';

            const data = await this.request(endpoint);
            return data;
        } catch (error) {
            console.error('Error fetching products:', error);
            return { success: false, error: error.message, data: [] };
        }
    },

    async getProductById(id) {
        try {
            const data = await this.request(`/products/${id}`);
            return data;
        } catch (error) {
            return { success: false, error: error.message };
        }
    },

    async createProduct(productData) {
        try {
            const data = await this.request('/products', {
                method: 'POST',
                body: JSON.stringify(productData),
            });
            return data;
        } catch (error) {
            return { success: false, error: error.message };
        }
    },

    async updateProduct(id, updates) {
        try {
            const data = await this.request(`/products/${id}`, {
                method: 'PUT',
                body: JSON.stringify(updates),
            });
            return data;
        } catch (error) {
            return { success: false, error: error.message };
        }
    },

    async deleteProduct(id) {
        try {
            const data = await this.request(`/products/${id}`, {
                method: 'DELETE',
            });
            return data;
        } catch (error) {
            return { success: false, error: error.message };
        }
    },

    async getProductStats() {
        try {
            const data = await this.request('/products/stats');
            return data;
        } catch (error) {
            return { success: false, error: error.message };
        }
    },

    // ==================== ORDERS ====================

    async getAllOrders(filters = {}) {
        try {
            const queryParams = new URLSearchParams();
            if (filters.status) queryParams.append('status', filters.status);
            if (filters.startDate) queryParams.append('startDate', filters.startDate);
            if (filters.endDate) queryParams.append('endDate', filters.endDate);
            if (filters.sort) queryParams.append('sort', filters.sort);

            const queryString = queryParams.toString();
            const endpoint = queryString ? `/orders?${queryString}` : '/orders';

            const data = await this.request(endpoint);
            return data;
        } catch (error) {
            console.error('Error fetching orders:', error);
            return { success: false, error: error.message, data: [] };
        }
    },

    async getOrderById(id) {
        try {
            const data = await this.request(`/orders/${id}`);
            return data;
        } catch (error) {
            return { success: false, error: error.message };
        }
    },

    async createOrder(orderData) {
        try {
            const data = await this.request('/orders', {
                method: 'POST',
                body: JSON.stringify(orderData),
            });
            return data;
        } catch (error) {
            return { success: false, error: error.message };
        }
    },

    async updateOrder(id, updates) {
        try {
            const data = await this.request(`/orders/${id}`, {
                method: 'PUT',
                body: JSON.stringify(updates),
            });
            return data;
        } catch (error) {
            return { success: false, error: error.message };
        }
    },

    async deleteOrder(id) {
        try {
            const data = await this.request(`/orders/${id}`, {
                method: 'DELETE',
            });
            return data;
        } catch (error) {
            return { success: false, error: error.message };
        }
    },

    async getOrderStats() {
        try {
            const data = await this.request('/orders/stats');
            return data;
        } catch (error) {
            return { success: false, error: error.message };
        }
    },

    async getAnalytics(period = 12) {
        try {
            const data = await this.request(`/orders/analytics?period=${period}`);
            return data;
        } catch (error) {
            return { success: false, error: error.message };
        }
    },

    // Sales Analytics (NEW)
    async getSalesAnalytics(startDate, endDate, interval = 'daily') {
        try {
            const queryParams = new URLSearchParams();
            if (startDate) queryParams.append('startDate', startDate);
            if (endDate) queryParams.append('endDate', endDate);
            if (interval) queryParams.append('interval', interval);

            const queryString = queryParams.toString();
            const endpoint = queryString ? `/admin/sales-analytics?${queryString}` : '/admin/sales-analytics';

            const data = await this.request(endpoint);
            return data;
        } catch (error) {
            console.error('Error fetching sales analytics:', error);
            return { success: false, error: error.message };
        }
    },

    // ==================== UTILITY FUNCTIONS ====================

    // Check API health
    async checkHealth() {
        try {
            const response = await fetch(`${API_BASE_URL.replace('/api', '')}/api/health`);
            const data = await response.json();
            return data;
        } catch (error) {
            console.error('API Health Check Failed:', error);
            return { success: false, error: 'Backend server is not running' };
        }
    },
};

// Initialize on load
ShirtifyAPI.init();

// Backward compatibility wrapper for existing code
// This maintains the same interface as the old ShirtifyData object
const ShirtifyData = {
    products: [],
    orders: [],
    users: [], // Added users array
    currentUser: null,
    analyticsData: {
        dates: ['Oct 2023', 'Nov 2023', 'Dec 2023', 'Jan 2024', 'Feb 2024', 'Mar 2024',
            'Apr 2024', 'May 2024', 'Jun 2024', 'Jul 2024', 'Aug 2024', 'Sep 2024'],
        units: [420, 385, 450, 380, 425, 480, 510, 565, 620, 640, 595, 580],
        revenue: [1050000, 962500, 1125000, 950000, 1062500, 1200000,
            1275000, 1412500, 1550000, 1600000, 1487500, 1450000],
        products: [
            { name: 'Casual Round Neck', category: 'Casual', size: 'M', color: 'Black', units: 1250, revenue: 3125000, price: 2500, trend: '+15%' },
            { name: 'Casual V-Neck', category: 'Casual', size: 'L', color: 'White', units: 980, revenue: 2450000, price: 2500, trend: '+12%' },
            { name: 'Sports Performance', category: 'Sports', size: 'L', color: 'Blue', units: 850, revenue: 2550000, price: 3000, trend: '+22%' },
            { name: 'Graphic Print Urban', category: 'Graphic', size: 'M', color: 'Multi', units: 780, revenue: 2340000, price: 3000, trend: '+18%' },
            { name: 'Formal Polo', category: 'Formal', size: 'M', color: 'Navy', units: 720, revenue: 2520000, price: 3500, trend: '+8%' },
        ]
    },

    // Initialize - load data from API
    async init() {
        try {
            const productsResponse = await ShirtifyAPI.getAllProducts();
            if (productsResponse.success) {
                // Map products to the format expected by the dashboard
                this.products = (productsResponse.data || []).map(product => ({
                    id: product._id,
                    designName: product.name,
                    name: product.name,
                    description: product.description,
                    price: product.price,
                    image: product.image.startsWith('http') ? product.image : '../' + product.image,
                    category: product.category,
                    stock: product.stock !== undefined ? product.stock : 50,
                    stockBadge: product.stock < 10 ? 'Low Stock' : 'In Stock',
                    stockBadgeClass: product.stock < 10 ? 'bg-red-100 text-red-700 border-red-200' : 'bg-emerald-100 text-emerald-700 border-emerald-200',
                    featured: product.featured || false,
                    isCustom: product.isCustom || false
                }));
            }

            const ordersResponse = await ShirtifyAPI.getAllOrders();
            if (ordersResponse.success) {
                // Map orders to the format expected by the dashboard
                this.orders = (ordersResponse.data || []).map(order => {
                    const customerName = order.customer ? (typeof order.customer === 'string' ? order.customer : (order.customer.name || "Customer")) :
                        (order.shippingAddress ? order.shippingAddress.fullName : "Guest");

                    const location = order.customer && order.customer.city ? order.customer.city :
                        (order.shippingAddress ? order.shippingAddress.city : "Unknown");

                    const itemsSummary = Array.isArray(order.items) ?
                        `${order.items.length} item${order.items.length !== 1 ? 's' : ''} (${order.items.map(i => i.name || (i.product ? i.product.name : 'Product')).join(', ')})` :
                        (order.items || "No items");

                    const statusBadgeClass = {
                        'Pending': 'bg-amber-100 text-amber-700 border-amber-200',
                        'Processing': 'bg-blue-100 text-blue-700 border-blue-200',
                        'Shipped': 'bg-purple-100 text-purple-700 border-purple-200',
                        'Delivered': 'bg-emerald-100 text-emerald-700 border-emerald-200',
                        'Cancelled': 'bg-red-100 text-red-700 border-red-200',
                        'pending': 'bg-amber-100 text-amber-700 border-amber-200',
                        'processing': 'bg-blue-100 text-blue-700 border-blue-200',
                        'shipped': 'bg-purple-100 text-purple-700 border-purple-200',
                        'delivered': 'bg-emerald-100 text-emerald-700 border-emerald-200'
                    };

                    const paymentBadgeClass = {
                        'cash-on-delivery': 'bg-gray-100 text-gray-700 border-gray-200',
                        'jazzcash': 'bg-red-100 text-red-700 border-red-200',
                        'easypaisa': 'bg-emerald-100 text-emerald-700 border-emerald-200',
                        'bank-transfer': 'bg-blue-100 text-blue-700 border-blue-200',
                        'card': 'bg-indigo-100 text-indigo-700 border-indigo-200'
                    };

                    const status = order.status ? (order.status.charAt(0).toUpperCase() + order.status.slice(1)) : 'Pending';

                    return {
                        id: order.orderNumber || order._id.toString().slice(-8).toUpperCase(),
                        realId: order._id,
                        customer: customerName,
                        location: location,
                        items: itemsSummary.length > 35 ? itemsSummary.substring(0, 32) + '...' : itemsSummary,
                        amount: order.totalAmount || order.totalPrice || 0,
                        payment: order.paymentMethod || 'COD',
                        paymentBadge: paymentBadgeClass[order.paymentMethod] || 'bg-gray-100 text-gray-700 border-gray-200',
                        status: status,
                        statusBadge: statusBadgeClass[order.status] || 'bg-gray-100 text-gray-700 border-gray-200',
                        orderDate: order.createdAt
                    };
                });
            }

            // Load users
            const usersResponse = await ShirtifyAPI.getAllUsers();
            if (usersResponse.success) {
                this.users = (usersResponse.data || []).map(user => ({
                    id: user._id,
                    name: user.name,
                    email: user.email,
                    role: user.role || 'User',
                    status: 'Active',
                    lastActive: 'Online'
                }));
            }

            // Load analytics
            const analyticsResponse = await ShirtifyAPI.getAnalytics();
            if (analyticsResponse.success && analyticsResponse.data) {
                const { timeline, products } = analyticsResponse.data;

                if (timeline && timeline.length > 0) {
                    this.analyticsData.dates = timeline.map(item => item.date);
                    this.analyticsData.units = timeline.map(item => item.units);
                    this.analyticsData.revenue = timeline.map(item => item.revenue);
                }

                if (products) {
                    this.analyticsData.products = products;
                }
            }

            this.currentUser = ShirtifyAPI.currentUser;
        } catch (error) {
            console.error('Error initializing data:', error);
        }
    },

    // Authentication
    async authenticateUser(email, password) {
        const result = await ShirtifyAPI.login(email, password);
        if (result.success) {
            this.currentUser = result.data;
            return { success: true, user: result.data };
        }
        return { success: false, message: result.error };
    },

    logoutUser() {
        ShirtifyAPI.logout();
        this.currentUser = null;
    },

    isAuthenticated() {
        return ShirtifyAPI.isAuthenticated();
    },

    getCurrentUser() {
        return ShirtifyAPI.currentUser;
    },

    // Product management
    async addProduct(product) {
        const result = await ShirtifyAPI.createProduct(product);
        if (result.success) {
            this.products.push(result.data);
        }
        return result.data;
    },

    async updateProduct(productId, updates) {
        const result = await ShirtifyAPI.updateProduct(productId, updates);
        if (result.success) {
            const index = this.products.findIndex(p => p._id === productId);
            if (index !== -1) {
                this.products[index] = result.data;
            }
        }
        return result.data;
    },

    async deleteProduct(productId) {
        const result = await ShirtifyAPI.deleteProduct(productId);
        if (result.success) {
            this.products = this.products.filter(p => p._id !== productId);
        }
        return result.success;
    },

    // Order management
    async addOrder(order) {
        const result = await ShirtifyAPI.createOrder(order);
        if (result.success) {
            this.orders.push(result.data);
        }
        return result.data;
    },

    async updateOrderStatus(orderId, newStatus) {
        const result = await ShirtifyAPI.updateOrder(orderId, { status: newStatus });
        if (result.success) {
            const index = this.orders.findIndex(o => o._id === orderId);
            if (index !== -1) {
                this.orders[index] = result.data;
            }
        }
        return result.data;
    },

    // Analytics
    async getKPIs() {
        const statsResponse = await ShirtifyAPI.getProductStats();
        const orderStatsResponse = await ShirtifyAPI.getOrderStats();

        if (statsResponse.success && orderStatsResponse.success) {
            return {
                totalProducts: statsResponse.data.totalProducts,
                totalOrders: orderStatsResponse.data.totalOrders,
                totalSales: orderStatsResponse.data.totalRevenue,
                totalCustomers: this.orders.length, // Simplified
            };
        }

        return {
            totalProducts: this.products.length,
            totalOrders: this.orders.length,
            totalSales: 0,
            totalCustomers: 0,
        };
    },

    getFilteredProducts(filters = {}) {
        // Client-side filtering for now
        let filtered = [...this.products];

        if (filters.color) {
            filtered = filtered.filter(p => p.colors && p.colors.includes(filters.color));
        }

        if (filters.size) {
            filtered = filtered.filter(p => p.sizes && p.sizes.includes(filters.size));
        }

        if (filters.minPrice) {
            filtered = filtered.filter(p => p.price >= filters.minPrice);
        }

        if (filters.maxPrice) {
            filtered = filtered.filter(p => p.price <= filters.maxPrice);
        }

        if (filters.category) {
            filtered = filtered.filter(p => p.category === filters.category);
        }

        return filtered;
    },
};

// Export for use in other files
if (typeof module !== 'undefined' && module.exports) {
    module.exports = { ShirtifyAPI, ShirtifyData };
}
const express = require('express');
const router = express.Router();
const Order = require('../models/Order');
const Product = require('../models/Product');
const mongoose = require('mongoose');

/**
 * @route   GET /api/admin/sales-analytics
 * @desc    Get top products, size distribution, and sales trends
 * @access  Admin
 */
router.get('/sales-analytics', async (req, res) => {
    try {
        const { startDate, endDate, interval = 'daily' } = req.query;
        
        // Date range filter
        const dateFilter = {};
        if (startDate || endDate) {
            dateFilter.createdAt = {};
            if (startDate) dateFilter.createdAt.$gte = new Date(startDate);
            if (endDate) dateFilter.createdAt.$lte = new Date(endDate);
        }

        // 1. Top-Selling T-shirt Designs (by order count)
        const topDesigns = await Order.aggregate([
            { $match: dateFilter },
            { $unwind: '$items' },
            {
                $group: {
                    _id: '$items.productName',
                    count: { $sum: 1 },
                    quantity: { $sum: '$items.quantity' },
                    revenue: { $sum: { $multiply: ['$items.quantity', '$items.price'] } }
                }
            },
            { $sort: { count: -1 } },
            { $limit: 10 }
        ]);

        // 2. Most Popular Sizes (S/M/L/XL distribution)
        const sizeDistribution = await Order.aggregate([
            { $match: dateFilter },
            { $unwind: '$items' },
            {
                $group: {
                    _id: '$items.size',
                    count: { $sum: '$items.quantity' }
                }
            },
            { $match: { _id: { $in: ['S', 'M', 'L', 'XL', 'XXL'] } } },
            { $sort: { _id: 1 } }
        ]);

        // 3. Order Trends (Line chart data)
        const dateGrouping = interval === 'weekly' 
            ? { $dateToString: { format: "%Y-W%U", date: "$createdAt" } }
            : { $dateToString: { format: "%Y-%m-%d", date: "$createdAt" } };

        const trends = await Order.aggregate([
            { $match: dateFilter },
            {
                $group: {
                    _id: dateGrouping,
                    orderCount: { $sum: 1 },
                    totalRevenue: { $sum: '$totalAmount' },
                    date: { $first: '$createdAt' }
                }
            },
            { $sort: { date: 1 } }
        ]);

        res.json({
            success: true,
            data: {
                topDesigns,
                sizeDistribution,
                trends
            }
        });

    } catch (error) {
        console.error('Sales analytics error:', error);
        res.status(500).json({
            success: false,
            message: 'Server error while fetching sales analytics',
            error: error.message
        });
    }
});

module.exports = router;
const express = require('express');
const path = require('path');
const dotenv = require('dotenv');
const cors = require('cors');
const connectDB = require('./config/db');
const errorHandler = require('./middleware/errorHandler');

// Load environment variables
dotenv.config();

// Connect to database (Disable auto-connect in test mode)
if (process.env.NODE_ENV !== 'test') {
    connectDB();
}

// Initialize Express app
const app = express();

// Body parser middleware
app.use(express.json());
app.use(express.urlencoded({ extended: true }));

// Enable CORS
app.use(cors({
    origin: true,
    credentials: true,
}));

// ===== SERVE STATIC FILES =====

// 1. Serve Storefront (Root)
app.use(express.static(path.join(__dirname, '../frontend')));

// 2. Serve Admin Panel (Sub-route)
app.use('/admin', express.static(path.join(__dirname, '../frontend/admin')));

// ===== API ROUTES =====

// Authentication (JWT-based)
app.use('/api/auth', require('./routes/authRoutes'));
app.use('/api/auth', require('./routes/passwordReset'));

// Products (NEW - Dynamic)
app.use('/api/products', require('./routes/productRoutes'));
app.use('/api/designs', require('./routes/designRoutes'));

// Orders (Unified)
app.use('/api/orders', require('./routes/orderRoutes'));

// Payments (Storefront)
app.use('/api/payments', require('./routes/payments'));

// AI Virtual Try-on
app.use('/api/ai', require('./routes/aiRoutes'));

// Feedback (Storefront)
app.use('/api/feedback', require('./routes/feedback'));

// ML Sales Prediction
app.use('/api/ml', require('./routes/mlRoutes'));

// Admin Routes (NEW)
app.use('/api/admin', require('./routes/adminRoutes'));

// Health check
app.get('/api/health', (req, res) => {
    res.status(200).json({
        success: true,
        message: 'Shirtify Unified API is running',
        timestamp: new Date().toISOString(),
        environment: process.env.NODE_ENV || 'development',
    });
});

// ===== FALLBACK ROUTES =====

// Serve storefront index.html for root
app.get('/', (req, res) => {
    res.sendFile(path.join(__dirname, '../frontend/homescreen/homescreen.html'));
});

// 404 handler
app.use((req, res) => {
    res.status(404).json({
        success: false,
        error: 'Route not found',
    });
});

// Error handler middleware (must be last)
app.use(errorHandler);

const PORT = process.env.PORT || 5000;

if (require.main === module) {
    const serverInstance = app.listen(PORT, () => {
        console.log(`\n${'='.repeat(60)}`);
        console.log(`🚀 Shirtify Unified E-Commerce Platform`);
        console.log(`${'='.repeat(60)}`);
        console.log(`📦 Environment: ${process.env.NODE_ENV || 'development'}`);
        console.log(`🌐 Server running on port ${PORT}`);
        console.log(`\n🛍️  Storefront:   http://localhost:${PORT}/`);
        console.log(`⚙️  Admin Panel:  http://localhost:${PORT}/admin/`);
        console.log(`🔌 API Health:   http://localhost:${PORT}/api/health`);
        console.log(`${'='.repeat(60)}\n`);
    });

    // Handle unhandled promise rejections
    process.on('unhandledRejection', (err) => {
        console.log(`❌ Error: ${err.message}`);
        serverInstance.close(() => process.exit(1));
    });
}

module.exports = app;
from flask import Flask, request, jsonify
from flask_cors import CORS
from model import SalesPredictionModel
import os
import sys

app = Flask(__name__)
CORS(app)

# Initialize model
ml_model = SalesPredictionModel()

# Try to load existing trained model
MODEL_PATH = os.path.join(os.path.dirname(__file__), 'trained_model.pkl')
try:
    ml_model.load_model(MODEL_PATH)
    print(f"✅ Loaded trained model with accuracy: {ml_model.accuracy:.2%}")
except FileNotFoundError:
    print("⚠️ No trained model found. Train the model first using train.py")

@app.route('/health', methods=['GET'])
def health_check():
    """Health check endpoint"""
    return jsonify({
        'status': 'healthy',
        'model_trained': ml_model.is_trained,
        'accuracy': ml_model.accuracy if ml_model.is_trained else None
    })

@app.route('/train', methods=['POST'])
def train_model():
    """
    Train the model with provided historical data
    
    Expected JSON body:
    {
        "historical_data": [
            {"date": "2024-01-01", "totalAmount": 5000},
            {"date": "2024-01-02", "totalAmount": 6200},
            ...
        ]
    }
    """
    try:
        data = request.get_json()
        
        if not data or 'historical_data' not in data:
            return jsonify({
                'success': False,
                'error': 'Missing historical_data in request body'
            }), 400
        
        historical_data = data['historical_data']
        
        if len(historical_data) < 10:
            return jsonify({
                'success': False,
                'error': 'Need at least 10 data points to train model'
            }), 400
        
        # Train model
        training_results = ml_model.train(historical_data)
        
        # Save model
        ml_model.save_model(MODEL_PATH)
        
        return jsonify({
            'success': True,
            'message': 'Model trained successfully',
            'results': training_results,
            'accuracy': ml_model.accuracy,
            'model_saved': True
        })
    
    except Exception as e:
        return jsonify({
            'success': False,
            'error': str(e)
        }), 500

@app.route('/predict', methods=['POST'])
def predict_sales():
    """
    Predict future sales
    
    Expected JSON body:
    {
        "days_ahead": 30,
        "start_date": "2024-03-01" (optional)
    }
    """
    try:
        if not ml_model.is_trained:
            return jsonify({
                'success': False,
                'error': 'Model not trained. Please train the model first.'
            }), 400
        
        data = request.get_json() or {}
        days_ahead = data.get('days_ahead', 30)
        start_date = data.get('start_date', None)
        
        # Validate days_ahead
        if not isinstance(days_ahead, int) or days_ahead < 1 or days_ahead > 365:
            return jsonify({
                'success': False,
                'error': 'days_ahead must be between 1 and 365'
            }), 400
        
        # Make predictions
        predictions = ml_model.predict(days_ahead, start_date)
        
        # Calculate summary statistics
        total_predicted = sum(p['predicted_sales'] for p in predictions)
        avg_daily_sales = total_predicted / len(predictions)
        
        return jsonify({
            'success': True,
            'predictions': predictions,
            'summary': {
                'total_predicted_sales': round(total_predicted, 2),
                'average_daily_sales': round(avg_daily_sales, 2),
                'prediction_period_days': days_ahead
            },
            'model_accuracy': ml_model.accuracy,
            'features_used': ml_model.features_used
        })
    
    except Exception as e:
        return jsonify({
            'success': False,
            'error': str(e)
        }), 500

@app.route('/model-info', methods=['GET'])
def model_info():
    """Get information about the current model"""
    return jsonify({
        'is_trained': ml_model.is_trained,
        'accuracy': ml_model.accuracy if ml_model.is_trained else None,
        'features_used': ml_model.features_used,
        'model_type': 'Linear Regression'
    })

if __name__ == '__main__':
    port = int(os.environ.get('ML_SERVICE_PORT', 5001))
    print(f"\n🚀 ML Service starting on port {port}")
    print(f"📊 Model trained: {ml_model.is_trained}")
    if ml_model.is_trained:
        print(f"✨ Model accuracy: {ml_model.accuracy:.2%}\n")
    
    app.run(host='0.0.0.0', port=port, debug=False)
